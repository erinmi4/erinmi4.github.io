---
title: 行列变换和方程的解
description: 总结自《新版演习线性代数_寺田文行》一书，使用最小笔记的形式去记录。
pubDate: 2026-04-15
updatedDate: 2026-04-15
tags:
  - 线性代数
  - 修考
category: 修考
draft: false
---

[TOC]



### 通解的结构 (Complete Solution)

对于一般的线性方程组 $Ax=b$，其**完全解 (Complete Solution)** 的结构由两部分组成：
$$x = x_p + x_n$$

*   **$x_p$ (特解, Particular Solution)**：满足 $Ax=b$ 的任意一个特定解。通常可以通过将所有自由变数设为 0 来求得。
*   **$x_n$ (齐次解/零空间解, Homogeneous Solution)**：来自零空间 $N(A)$ 的任意向量，满足 $Ax=0$。

### 解方程

求解$AX=b$的$X$

1. 克拉默法则：通过[Cramer's rule](Cramer's rule)，将待求参数的一列使用b替换掉就可以
   - 对于线性方程组 $A\mathbf{x} = \mathbf{d}$，若 $|A| \neq 0$，则解 $x_i$ 为：
         $$x_i = \frac{| \mathbf{b}_1 \cdots \mathbf{d} \cdots \mathbf{b}_n |}{|A|}$$
         即：分子是将 $A$ 的第 $i$ 列替换为右侧常数向量 $\mathbf{d}$ 后得到的行列式。

2. 使用逆矩阵的方式， $AX=b \rightarrow X=A^{-1}b$

3. 使用高斯消元法来构造行阶梯型

![image-20260415100316383](./%E7%BA%BF%E6%80%A7%E4%BB%A3%E6%95%B0Chapter3.assets/image-20260415100316383.png)

#### 表格法

通过表格法，我们能够记录矩阵变换的操作

- 构造增广矩阵$[A|E]$
- 然后不断记录初等矩阵的变换操作，如果将$A$变成$E$,那么$E$就记录了$A^{-1}$的操作

### 解的个数

解的个数有三种情况

1. 唯一解
2. 无穷解
3. 无解

#### 我们从四个基本空间的视角来理解这个问题

**无解**：如果 $b$ 不在**列空间 $C(A)$** 内。

- 这意味着 $b$ 无法由 $A$ 的列线性组合得出。
  - 在化简过程中，出现了第三行全为 $0$ 但对应的常数项为 $1$ 的情况（即 $0x+0y+0z+0u=1$）。
  - **结论：** 这种矛盾说明方程组**无解**。

**唯一解**：如果 $b$ 在**列空间 $C(A)$** 内，且**零空间 $N(A)$ 只有零向量**。

- 这对应于**列满秩**（Full column rank），即所有的列都是线性无关的，没有自由变量。

**无穷多解**：如果 $b$ 在**列空间 $C(A)$** 内，且**零空间 $N(A)$ 包含非零向量**。

- 这意味着矩阵存在**自由变量**（Free variables），你可以通过这些自由变量构造出无数个特解。

#### 判别标准

$\text{rank } A = \text{rank } [A | b] = n$ $\Rightarrow$ **唯一解**。

$\text{rank } A = \text{rank } [A | b] < n$ $\Rightarrow$ **无穷多解**。

$\text{rank } A < \text{rank } [A | b]$ $\Rightarrow$ **无解**。



## LU分解法

*   **消去法的矩阵形式：**
    *   高斯消去法的每一步行操作（如：第二列减去 2 倍的第一列），都可以表示为乘以一个**初等矩阵（Elimination Matrix, E）**。
    *   例如：$E_{21} A$ 表示对 $A$ 进行操作，消去位置 (2,1) 的元素。
    *   整个消去过程可以表示为一连串矩阵相乘：$G F E A = U$。
    *   这一连串的操作可以等效为 一个变换 $P$ 也就是 $PA=U$



因为$P$是由初等矩阵组成的。

#### 初等矩阵的逆矩阵

- 初等矩阵本质上就是行图像上的行变换

- 初等矩阵的逆矩阵

  - 修改在主对角线上：取倒数

  - 修改在非主对角线上：取相反数

    $P_I^{-1}$ (对应将某一行乘上非零常数 $c$): 它的逆操作是乘以 $c^{-1}$ (即 $1/c$)。        

    $P_{I\hspace{-.1em}I}^{-1} = P_{I\hspace{-.1em}I}$ (对应交换两行): 它的逆操作就是再交换一次，所以逆矩阵就是它自己。        

    $P_{I\hspace{-.1em}I\hspace{-.1em}I}^{-1}$ (对应把第 $j$ 行的 $c$ 倍加到第 $i$ 行): 它的逆操作是把第 $j$ 行的 $-c$ 倍加到第 $i$ 行。

