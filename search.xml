<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>CS144 checkpoint 1</title>
      <link href="/2025/02/19/ji-suan-ji-wang-luo-cs144-checkpoint1/"/>
      <url>/2025/02/19/ji-suan-ji-wang-luo-cs144-checkpoint1/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      <categories>
          
          <category> 计算机网络 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> lab </tag>
            
            <tag> 计算机网络 </tag>
            
            <tag> Stanford CS144 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>FreeRtos lec8 队列集</title>
      <link href="/2025/02/19/freertos-lec8/"/>
      <url>/2025/02/19/freertos-lec8/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      <categories>
          
          <category> FREERTOS </category>
          
      </categories>
      
      
        <tags>
            
            <tag> lab </tag>
            
            <tag> 嵌入式 </tag>
            
            <tag> 韦东山FREERTOS </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Lab4 traps</title>
      <link href="/2025/02/18/lab4-traps/"/>
      <url>/2025/02/18/lab4-traps/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 操作系统 </tag>
            
            <tag> MIT6.s081 </tag>
            
            <tag> lab </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Chapter 4 陷阱————系统调用，异常和硬件中断</title>
      <link href="/2025/02/18/cao-zuo-xi-tong-lec4/"/>
      <url>/2025/02/18/cao-zuo-xi-tong-lec4/</url>
      
        <content type="html"><![CDATA[<p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250219153823338.png" alt="syscall"></p><p>在这一章，主要描述的是用户空间与内核空间的交互。</p><p>这也就引起了我们在chapter2中讨论的三个问题<strong>复用</strong>（共享资源）、<strong>隔离</strong>（防止进程互相干扰）、<strong>交互</strong>（允许进程间的通信）。</p><p>我们将会使用虚拟空间来实现。</p><p>通过跳板页，我们实现了一个物理区域，能够同时被两种空间所映射到，都能读取和写入。</p><p>通过对跳板页中内容的读取，避免了空间的直接接触，这也是一种分层的思想。</p><hr><p>有三种事件会导致CPU中断普通指令的执行</p><ol><li>系统调用ecall，会使得将权限转为内核，执行内核指令</li><li>异常Exception，一条指令（无论是用户态还是内核态）执行了非法操作<ol><li>例如除以零或使用无效的虚拟地址。</li></ol></li><li>设备中断<ol><li>对于一些读写请求，需要即时处理，不然缓冲区满了之后，会导致一些数据的丢失</li></ol></li></ol><p>对于<strong>陷阱</strong>，通常，发生陷阱时正在执行的代码稍后需要恢复执行，并且不应意识到发生了任何特殊事件。也就是说，我们通常希望陷阱是透明的。</p><p>通常的流程是：</p><ul><li>陷阱强制将控制权转移到内核；</li><li>内核保存寄存器和其他状态以便恢复执行；<strong>保留上下文</strong></li><li>内核执行适当的处理代码（例如系统调用的实现或设备驱动程序）；</li><li>内核恢复保存的状态并从陷阱返回；<strong>恢复现场</strong></li><li>原始代码从它离开的地方继续执行。</li></ul><p>我们要先回答一个问题，为什么陷阱要在内核中执行。</p><ol><li>对于ecall，这本身就需要提高权限才能执行内核操作。</li><li>对于Exception，我们一般执行panic，将异常的进程直接kill</li><li>对于设备中断，一般诸如读写操作等，使用内核空间相较于user space的反应更快</li></ol><blockquote><ul><li><strong>从用户模式切换到监督模式允许什么？</strong><ul><li>监督模式可以使用CPU控制寄存器：<ul><li><code>satp</code>：页表物理地址。</li><li><code>stvec</code>：<code>ecall</code>跳转到内核的地址；指向跳板页。</li><li><code>sepc</code>：<code>ecall</code>将用户<code>pc</code>保存在此。</li><li><code>sscratch</code>：陷阱帧的地址。</li></ul></li><li>监督模式可以使用没有<code>PTE_U</code>标志的PTE。</li><li>但监督模式没有其他权限！<ul><li>例如，不能使用不在页表中的地址。</li><li>因此，内核必须仔细设置，以便它能够工作。</li></ul></li></ul></li></ul></blockquote><p>第二如何执行<strong>陷阱操作</strong>handler</p><p>这三种陷阱类型的共性表明内核可以用单一代码路径处理所有陷阱，但事实证明，为三种不同情况<strong>分别编写代码</strong>更为方便：来自用户空间的陷阱、来自内核空间的陷阱和时钟中断。</p><blockquote><p>处理陷阱的内核代码（无论是汇编还是C）通常被称为“处理程序”（handler）；最初的处理程序指令通常用汇编语言编写（而不是C语言），有时被称为“向量”（vector）。</p></blockquote><p>Xv6的陷阱处理分为四个阶段：</p><ul><li>RISC-V CPU执行的硬件操作、</li><li>一些为内核C代码做准备的汇编指令、</li><li>一个决定如何处理陷阱的C函数，</li><li>以及系统调用或设备驱动服务例程</li></ul><h3 id="4-1-RISC-V陷阱机制（硬件准备）"><a href="#4-1-RISC-V陷阱机制（硬件准备）" class="headerlink" title="4.1 RISC-V陷阱机制（硬件准备）"></a>4.1 RISC-V陷阱机制（硬件准备）</h3><p>每个RISC-V CPU都有一组<strong>控制寄存器</strong>，内核可以写入这些寄存器以告诉CPU<strong>如何处理陷阱</strong>，也可以读取这些寄存器以<strong>了解已发生的陷阱</strong>。</p><p><strong>标记陷阱状态，记录上下文，保护现场</strong></p><p>因为我们会为三种不同情况，分别编写代码，所以我们要能够实现对于情况的区分</p><p>以下是最重要的寄存器概述：</p><ul><li><strong>stvec</strong>：内核将陷阱处理程序的地址写入此寄存器；RISC-V在处理陷阱时会<strong>跳转</strong>到<code>stvec</code>中的地址。</li><li><strong>sepc</strong>：当陷阱发生时，RISC-V将程序计数器（PC）的值保存在此寄存器中（因为PC随后会被<code>stvec</code>中的值覆盖）。<code>sret</code>（从陷阱返回）指令会将<code>sepc</code>的值复制到PC。内核也可以写入<code>sepc</code>以控制<code>sret</code>的目标地址。（<strong>保存现场</strong>）</li><li><strong>scause</strong>：RISC-V在此寄存器中存储一个数字，用于<strong>描述陷阱的原因</strong>。</li><li><strong>sscratch</strong>：内核在此寄存器中放置一个值，该值在陷阱处理程序的<strong>最开始阶段</strong>非常有用。(<strong>装载a0的值</strong>)</li><li><strong>sstatus</strong>：<code>sstatus</code>中的SIE位控制<strong>是否启用设备中断</strong>。如果内核清除SIE位，RISC-V将推迟设备中断，直到内核设置SIE位。SPP位指示陷阱是来自用户模式还是监督模式，并控制<code>sret</code>返回到哪种模式。（<strong>中断状态标志位</strong>）</li></ul><p>当需要<strong>强制触发陷阱</strong>时，RISC-V硬件对所有陷阱类型（除了时钟中断）执行以下操作：</p><ol><li>如果陷阱是设备中断，且<code>sstatus</code>中的SIE位被清除，则不执行以下操作。</li><li>通过清除<code>sstatus</code>中的SIE位来禁用中断。</li><li>将PC的值复制到<code>sepc</code>。</li><li>将当前模式（用户模式或监督模式）保存到<code>sstatus</code>中的SPP位。</li><li>设置<code>scause</code>以反映陷阱的原因。</li><li>将模式切换到监督模式。</li><li>将<code>stvec</code>的值复制到PC。（<strong>将控制权完全交给内核</strong>）</li><li>从新的PC地址开始执行。</li></ol><p>注意，CPU不会切换到内核页表，不会切换到内核栈，也不会保存任何寄存器，除了PC。这些任务必须由内核软件完成。</p><h3 id="4-2-来自用户空间的陷阱"><a href="#4-2-来自用户空间的陷阱" class="headerlink" title="4.2 来自用户空间的陷阱"></a>4.2 来自用户空间的陷阱</h3><p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20230923151811134.png" alt="high-level picture of the procedure of switching from user mode to supervisor mode"></p><p>用户态通过<code>syscall</code>切换到内核态，并执行系统调用的完整过程：</p><ol><li>用户态程序执行<code>ecall</code>指令。</li><li>触发软件中断，CPU保存当前寄存器状态到跳板页。</li><li>CPU跳转到<code>uservec</code>处理向量。</li><li><code>uservec</code>设置寄存器，跳转到<code>usertrap</code>。</li><li><code>usertrap</code>保存更多上下文信息，并调用<code>syscall</code>。</li><li><code>syscall</code>根据<code>a7</code>中的系统调用号，决定执行哪个系统调用处理函数。</li><li>执行相应的系统调用处理函数。</li><li>完成后，<code>syscall</code>返回，<code>usertrap</code>恢复上下文，跳回用户态继续执行。</li></ol><h4 id="“跳板页”（trampoline-page）"><a href="#“跳板页”（trampoline-page）" class="headerlink" title="“跳板页”（trampoline page）"></a>“跳板页”（trampoline page）</h4><p>Xv6陷阱处理设计的一个主要限制是：**RISC-V硬件在强制执行陷阱时不会切换页表。**这意味着<code>stvec</code>中的陷阱处理程序地址必须在用户页表中有有效的映射，因为当陷阱处理代码开始执行时，生效的是用户页表。此外，Xv6的陷阱处理代码需要切换到内核页表；为了在切换后能够继续执行，内核页表也必须对<code>stvec</code>指向的处理程序有映射。（<strong>这是一个在内存上，user space和kernel space拥有共同物理地址的地方</strong>）</p><p>Xv6通过一个“跳板页”（trampoline page）来满足这些要求。跳板页包含<code>uservec</code>，这是<code>stvec</code>指向的Xv6陷阱处理代码。跳板页在每个进程的页表中都映射到地址<code>TRAMPOLINE</code>，该地址位于虚拟地址空间的末尾，以便它位于程序为自己使用的内存之上。跳板页也在内核页表中映射到地址<code>TRAMPOLINE</code>。<strong>因此可以在监督模式下从那里开始执行陷阱处理。由于跳板页在内核地址空间中映射到相同的地址，因此在切换到内核页表后，陷阱处理程序可以继续执行。</strong></p><h4 id="uservec的执行"><a href="#uservec的执行" class="headerlink" title="uservec的执行"></a>uservec的执行</h4><blockquote><p>在RISC-V架构中，<code>a0</code>寄存器是一个重要的通用寄存器，主要用于函数调用和返回值的传递。</p></blockquote><ol><li><strong>交换了<code>a0</code>和<code>sscratch</code>的内容</strong></li></ol><p>当<code>uservec</code>开始执行时，所有32个寄存器都包含被中断的用户代码所拥有的值。这32个值需要被保存到内存中的某个地方，以便在陷阱返回用户空间时可以恢复它们。</p><p>将数据存储到内存需要使用一个寄存器来保存地址，但此时没有可用的通用寄存器！</p><p>幸运的是，RISC-V提供了一个帮助手段，即<code>sscratch</code>寄存器。<code>uservec</code>开头的<code>csrrw</code>指令交换了<code>a0</code>和<code>sscratch</code>的内容。</p><p>现在，用户代码的<code>a0</code>被保存在<code>sscratch</code>中；<code>uservec</code>有一个寄存器（<code>a0</code>）可以使用；并且<code>a0</code>包含内核之前放在<code>sscratch</code>中的值。</p><p><strong>因此，在交换<code>a0</code>和<code>sscratch</code>之后，<code>a0</code>持有当前进程的陷阱帧的指针。<code>uservec</code>现在将所有用户寄存器保存在那里，包括从<code>sscratch</code>读取的用户的<code>a0</code>。</strong></p><ol start="2"><li><strong>保存32个用户寄存器</strong></li></ol><p>在进入用户空间之前，内核将<code>sscratch</code>设置为指向每个进程的陷阱帧结构（trapframe），该结构（除其他内容外）有空间用于保存32个用户寄存器（<code>kernel/proc.h:44</code>）。</p><p>由于<code>satp</code>仍然指向用户页表，<code>uservec</code>需要陷阱帧在用户地址空间中被映射。</p><p>在创建每个进程时，Xv6为进程的陷阱帧分配一页，并安排它始终映射到用户虚拟地址<code>TRAPFRAME</code>，该地址位于<code>TRAMPOLINE</code>之下。</p><p>进程的<code>p-&gt;trapframe</code>也指向陷阱帧，尽管是通过其物理地址，因此内核可以通过内核页表使用它。</p><p><strong>每个进程在被创建的时候，就有一个trapframe的结构体被分配了，并且映射到跳板页上，这样可以满足所有进程的陷阱要求</strong></p><p><strong>陷阱帧包含当前进程的内核栈地址、当前CPU的<code>hartid</code>、<code>usertrap</code>函数的地址以及内核页表的地址。<code>uservec</code>检索这些值，将<code>satp</code>切换到内核页表，并调用<code>usertrap</code>。</strong></p><h4 id="usertrap的执行"><a href="#usertrap的执行" class="headerlink" title="usertrap的执行"></a>usertrap的执行</h4><p><code>usertrap</code>的工作是确定陷阱的原因，处理它并返回</p><ul><li>它首先更改<code>stvec</code>，以便在内核中发生的陷阱将由<code>kernelvec</code>而不是<code>uservec</code>处理。</li><li>它保存<code>sepc</code>寄存器（保存的用户程序计数器），因为<code>usertrap</code>可能会调用<code>yield</code>切换到另一个进程的内核线程，而该进程可能会返回用户空间，在这个过程中它会修改<code>sepc</code>。</li><li>处理陷阱<ul><li>如果陷阱是系统调用，<code>usertrap</code>调用<code>syscall</code>来处理；</li><li>如果是设备中断，则调用<code>devintr</code></li><li>否则是异常，内核会杀死违规进程。</li></ul></li><li>在退出时，<code>usertrap</code>检查进程是否已被杀死，或者是否应该让出CPU（如果这个陷阱是时钟中断）。</li></ul><h4 id="usertrapret和userret的执行"><a href="#usertrapret和userret的执行" class="headerlink" title="usertrapret和userret的执行"></a>usertrapret和userret的执行</h4><h5 id="1-usertrapret函数的作用"><a href="#1-usertrapret函数的作用" class="headerlink" title="1. usertrapret函数的作用"></a><strong>1. <code>usertrapret</code>函数的作用</strong></h5><p><code>usertrapret</code>是内核中的一个C函数，位于<code>kernel/trap.c:90</code>。它的主要任务是为返回用户空间做好准备，具体步骤如下：</p><h6 id="1-1-设置RISC-V控制寄存器"><a href="#1-1-设置RISC-V控制寄存器" class="headerlink" title="1.1 设置RISC-V控制寄存器"></a><strong>1.1 设置RISC-V控制寄存器</strong></h6><ul><li><strong>设置<code>stvec</code></strong>：将<code>stvec</code>寄存器设置为指向<code>uservec</code>。<code>uservec</code>是用户空间陷阱向量，用于处理用户空间的陷阱（如系统调用或异常）。</li><li><strong>准备陷阱帧字段</strong>：陷阱帧（<code>TRAPFRAME</code>）保存了用户态的上下文信息，包括寄存器状态和程序计数器（<code>sepc</code>）。</li><li><strong>设置<code>sepc</code></strong>：将<code>sepc</code>寄存器设置为之前保存的用户程序计数器值，以便在返回用户空间时恢复执行。</li></ul><h6 id="1-2-调用userret"><a href="#1-2-调用userret" class="headerlink" title="1.2 调用userret"></a><strong>1.2 调用<code>userret</code></strong></h6><p><code>usertrapret</code>最后调用<code>userret</code>函数，传递以下参数：</p><ul><li><strong><code>TRAPFRAME</code></strong>：通过<code>a0</code>寄存器传递，包含用户态的上下文信息。</li><li><strong>用户页表指针</strong>：通过<code>a1</code>寄存器传递，指向进程的用户页表。</li></ul><hr><h5 id="2-userret的作用"><a href="#2-userret的作用" class="headerlink" title="2. userret的作用"></a><strong>2. <code>userret</code>的作用</strong></h5><p><code>userret</code>是一个汇编函数，位于<code>kernel/trampoline.S:88</code>。它的主要任务是**切换到用户页表并恢复用户态的上下文。**以下是关键步骤：</p><h6 id="2-1-切换页表"><a href="#2-1-切换页表" class="headerlink" title="2.1 切换页表"></a><strong>2.1 切换页表</strong></h6><ul><li><strong>设置<code>satp</code></strong>：<code>userret</code>将<code>satp</code>寄存器切换到进程的用户页表。用户页表只映射了用户空间的内存和跳板页，而不映射内核的其他内容。</li><li><strong>跳板页的映射</strong>：跳板页在用户页表和内核页表中都映射到相同的虚拟地址。这使得<code>uservec</code>在切换<code>satp</code>后能够继续执行，而不会因为页表切换而导致地址失效。</li></ul><h6 id="2-2-恢复用户态上下文"><a href="#2-2-恢复用户态上下文" class="headerlink" title="2.2 恢复用户态上下文"></a><strong>2.2 恢复用户态上下文</strong></h6><ul><li><strong>复制用户<code>a0</code>到<code>sscratch</code></strong>：<code>userret</code>将陷阱帧中保存的用户<code>a0</code>值复制到<code>sscratch</code>寄存器。<code>sscratch</code>是一个临时寄存器，用于在内核和用户态之间交换数据。</li><li><strong>恢复用户寄存器</strong>：从陷阱帧中恢复保存的用户寄存器，包括通用寄存器和程序计数器。</li><li><strong>交换<code>a0</code>和<code>sscratch</code></strong>：进行最后一次交换，将用户的<code>a0</code>值恢复到<code>a0</code>寄存器，并将<code>TRAPFRAME</code>的地址保存到<code>sscratch</code>中，以便下一次陷阱发生时能够恢复上下文。</li><li><strong>执行<code>sret</code>指令</strong>：最后，<code>userret</code>执行<code>sret</code>指令，将控制权返回到用户空间，并恢复用户程序的执行。</li></ul><hr><h5 id="3-关键点总结"><a href="#3-关键点总结" class="headerlink" title="3. 关键点总结"></a><strong>3. 关键点总结</strong></h5><ol><li><strong>页表切换</strong>：<ul><li><code>userret</code>将<code>satp</code>切换到用户页表，但用户页表只映射用户空间和跳板页，不映射内核的其他内容。</li><li>跳板页在用户和内核页表中映射到相同的虚拟地址，确保切换后能够继续执行。</li></ul></li><li><strong>上下文恢复</strong>：<ul><li>用户态的寄存器状态保存在陷阱帧中。</li><li>通过<code>sscratch</code>寄存器交换<code>a0</code>值，确保用户态的<code>a0</code>被正确恢复，同时保存陷阱帧的地址。</li></ul></li><li><strong>返回用户空间</strong>：<ul><li><code>userret</code>通过<code>sret</code>指令返回用户空间，恢复用户程序的执行。</li></ul></li></ol><h3 id="4-3-代码：调用系统调用（如何触发）"><a href="#4-3-代码：调用系统调用（如何触发）" class="headerlink" title="4.3 代码：调用系统调用（如何触发）"></a>4.3 代码：调用系统调用（如何触发）</h3><p>在chapter2中，我们提到，ecall的执行是参数放入寄存器<code>a0</code>和<code>a1</code>中，并将系统调用号放入<code>a7</code>。</p><p>系统调用号与<code>syscalls</code>数组的条目匹配，这是一个函数指针表（<code>kernel/syscall.c:108</code>）。<code>ecall</code>指令触发进入内核。</p><p><code>syscall</code>（<code>kernel/syscall.c:133</code>）从<strong>陷阱帧中</strong>保存的<code>a7</code>中获取系统调用号，并用它来索引<code>syscalls</code>数组。对于第一个系统调用，<code>a7</code>包含<code>SYS_exec</code>（<code>kernel/syscall.h:8</code>），这将导致调用系统调用实现函数<code>sys_exec</code>。</p><p>当<code>sys_exec</code>返回时，<code>syscall</code>将其返回值记录在<code>p-&gt;trapframe-&gt;a0</code>中。这将导致原始的用户空间调用<code>exec()</code>返回该值，因为RISC-V的C调用约定将返回值放在<code>a0</code>中。系统调用通常返回负数表示错误，返回零或正数表示成功。如果系统调用号无效，<code>syscall</code>会打印错误并返回<code>-1</code>。</p><h3 id="4-4-代码：系统调用参数（如何传递）"><a href="#4-4-代码：系统调用参数（如何传递）" class="headerlink" title="4.4 代码：系统调用参数（如何传递）"></a>4.4 代码：系统调用参数（如何传递）</h3><p>内核中的系统调用实现需要找到用户代码传递的参数。</p><p>因为用户代码调用的是系统调用包装函数，所以参数最初位于RISC-V C调用约定指定的位置：寄存器中。</p><p><strong>内核陷阱代码将用户寄存器保存到当前进程的陷阱帧中</strong>，内核代码可以从那里找到它们。</p><p>内核函数<code>argint</code>、<code>argaddr</code>和<code>argfd</code>从陷阱帧中获取第<code>n</code>个系统调用参数，分别作为整数、指针或文件描述符。它们都调用<code>argraw</code>来获取相应的保存的用户寄存器（<code>kernel/syscall.c:35</code>）。</p><p>有些系统调用将指针作为参数传递，内核必须使用这些指针从用户内存中读取或写入数据。</p><p>例如，<code>exec</code>系统调用向内核传递一个指针数组，这些指针指向用户空间中的字符串参数。这些指针带来了两个挑战。首先，用户程序可能存在漏洞或恶意行为，可能会向内核传递无效指针，或者试图欺骗内核访问内核内存而不是用户内存。其次，Xv6内核页表映射与用户页表映射不同，因此内核不能使用普通指令从用户提供的地址加载或存储数据。</p><p>内核实现了可以安全地从用户提供的地址传输数据的函数。<code>fetchstr</code>是一个例子（<code>kernel/syscall.c:25</code>）。</p><p>文件系统调用（如<code>exec</code>）使用<code>fetchstr</code>从用户空间获取字符串文件名参数。<code>fetchstr</code>调用<code>copyinstr</code>来完成实际工作。</p><p><code>copyinstr</code>（<code>kernel/vm.c:398</code>）从用户页表<code>pagetable</code>中的虚拟地址<code>srcva</code>复制最多<code>max</code>字节到<code>dst</code>。由于<code>pagetable</code>不是当前页表，<code>copyinstr</code>使用<code>walkaddr</code>（它调用<code>walk</code>）在<code>pagetable</code>中查找<code>srcva</code>，得到物理地址<code>pa0</code>。</p><p>内核将每个物理RAM地址映射到相应的内核虚拟地址，因此<code>copyinstr</code>可以直接将字符串字节从<code>pa0</code>复制到<code>dst</code>。<code>walkaddr</code>（<code>kernel/vm.c:104</code>）检查用户提供的虚拟地址是否属于进程的用户地址空间，以防止程序欺骗内核读取其他内存。类似的函数<code>copyout</code>将数据从内核复制到用户提供的地址。</p><h3 id="4-5-来自内核空间的陷阱"><a href="#4-5-来自内核空间的陷阱" class="headerlink" title="4.5 来自内核空间的陷阱"></a>4.5 来自内核空间的陷阱</h3><p>Xv6根据当前执行的是用户代码还是内核代码，以不同的方式配置CPU的陷阱寄存器。</p><h5 id="kernelvec执行"><a href="#kernelvec执行" class="headerlink" title="kernelvec执行"></a>kernelvec执行</h5><p>当内核在一个CPU上执行时，内核将<code>stvec</code>指向<code>kernelvec</code>处的汇编代码（<code>kernel/kernelvec.S:10</code>）。因为Xv6已经在内核中，<code>kernelvec</code>可以依赖<code>satp</code>已经被设置为内核页表，并且栈指针指向一个有效的内核栈。<code>kernelvec</code>将所有32个寄存器推入栈中，稍后会从栈中恢复它们，以便被中断的内核代码可以无缝恢复执行。</p><p><code>kernelvec</code>将寄存器保存在被中断的内核线程的栈中，这是合理的，因为这些寄存器的值属于该线程。这在陷阱导致切换到不同线程时尤为重要——在这种情况下，陷阱实际上会从新线程的栈返回，而被中断线程的保存寄存器安全地留在它的栈上。</p><h5 id="kerneltrap执行"><a href="#kerneltrap执行" class="headerlink" title="kerneltrap执行"></a>kerneltrap执行</h5><p>保存寄存器后，<code>kernelvec</code>跳转到<code>kerneltrap</code>（<code>kernel/trap.c:134</code>）。<code>kerneltrap</code>为两种类型的陷阱做好了准备：设备中断和异常。它调用<code>devintr</code>（<code>kernel/trap.c:177</code>）来检查并处理设备中断。如果陷阱不是设备中断，那么它必定是异常，而如果在Xv6内核中发生异常，这总是致命错误；内核会调用<code>panic</code>并停止执行。</p><p>如果<code>kerneltrap</code>是由于时钟中断被调用的，并且一个进程的内核线程正在运行（而不是调度线程），<code>kerneltrap</code>会调用<code>yield</code>，以便其他线程有机会运行。在某个时刻，其中一个线程会<code>yield</code>，并让我们的线程及其<code>kerneltrap</code>再次恢复执行。第7章将解释<code>yield</code>中发生了什么。</p><h5 id="sret"><a href="#sret" class="headerlink" title="sret"></a>sret</h5><p>当<code>kerneltrap</code>的工作完成后，它需要返回到被陷阱中断的代码。因为<code>yield</code>可能会干扰<code>sepc</code>和<code>sstatus</code>中的前一个模式，所以<code>kerneltrap</code>在开始时保存了它们。现在它恢复这些控制寄存器并返回到<code>kernelvec</code>（<code>kernel/kernelvec.S:48</code>）。<code>kernelvec</code>从栈中弹出保存的寄存器并执行<code>sret</code>，它将<code>sepc</code>复制到<code>pc</code>，并恢复被中断的内核代码。</p><p>值得思考的是，如果<code>kerneltrap</code>由于时钟中断调用了<code>yield</code>，陷阱返回是如何发生的。</p><p><strong>要先禁用中断，然后设置stvec，不然在上下文切换操作的时候，会被设备中断打断</strong></p><p>当一个CPU从用户空间进入内核时，Xv6将该CPU的<code>stvec</code>设置为<code>kernelvec</code>；你可以在<code>usertrap</code>（<code>kernel/trap.c:29</code>）中看到这一点。存在一个时间窗口，内核已经开始执行，但<code>stvec</code>仍然设置为<code>uservec</code>，在这个窗口期间，必须确保不会发生设备中断。幸运的是，RISC-V在开始处理陷阱时总是禁用中断，Xv6直到设置<code>stvec</code>后才会再次启用它们。</p><h3 id="4-6-页面错误异常"><a href="#4-6-页面错误异常" class="headerlink" title="4.6 页面错误异常"></a>4.6 页面错误异常</h3><p><strong>Xv6对异常的响应相当简单：如果在用户空间发生异常，内核会杀死出错的进程；</strong></p><p><strong>如果在内核中发生异常，内核会进入恐慌状态。</strong></p><p>真实操作系统通常会有更有趣的响应方式。</p><h5 id="“写时复制”（Copy-on-Write，COW）"><a href="#“写时复制”（Copy-on-Write，COW）" class="headerlink" title="“写时复制”（Copy-on-Write，COW）"></a>“写时复制”（Copy-on-Write，COW）</h5><p>以页面错误为例，许多内核利用页面错误实现“写时复制”（Copy-on-Write，COW）的<code>fork</code>功能。</p><p><strong>写时复制，就是一种计算机的“懒惰操作”，将父子进程在一个物理地址内共享内存空间，但是这段空间被设置为仅读。一旦写入内容，就会触发panic，同时将这段内存复制到另一片物理区域上。实现父子进程的复制粘贴</strong></p><p>为了说明写时复制<code>fork</code>，考虑Xv6的<code>fork</code>，它在第3章中被描述过。</p><p><code>fork</code>会导致子进程的初始内存内容与父进程在<code>fork</code>时的内存内容相同。Xv6通过<code>uvmcopy</code>（<code>kernel/vm.c:301</code>）实现<code>fork</code>，它为子进程分配物理内存并将父进程的内存内容复制进去。</p><p>如果能让子进程和父进程共享父进程的物理内存，效率会更高。然而，这种直接的实现方式是不可行的，因为它会导致父进程和子进程在写入共享栈和堆时相互干扰。</p><p>通过合理使用页表权限和页面错误，父进程和子进程可以安全地共享物理内存。<strong>当虚拟地址在页表中没有映射，或者映射的<code>PTE_V</code>标志被清除，或者映射的权限位（<code>PTE_R</code>、<code>PTE_W</code>、<code>PTE_X</code>、<code>PTE_U</code>）禁止正在进行的操作时，CPU会触发页面错误异常。</strong></p><p><strong>RISC-V区分三种类型的页面错误：</strong></p><ol><li><p>加载页面错误（加载指令无法翻译其虚拟地址）</p></li><li><p>存储页面错误（存储指令无法翻译其虚拟地址）</p></li><li><p>指令页面错误（程序计数器中的地址无法翻译）。</p><p><code>scause</code>寄存器指示页面错误的类型，而<code>stval</code>寄存器包含无法翻译的地址。</p></li></ol><p>写时复制<code>fork</code>的基本计划是：</p><p>父进程和子进程最初共享所有物理页面，但每个进程都将它们映射为只读（清除<code>PTE_W</code>标志）。</p><p>父进程和子进程可以从共享的物理内存中读取。如果任一进程写入某个页面，RISC-V CPU会触发页面错误异常。</p><p>内核的陷阱处理程序会响应这一异常：分配一个新的物理页面，并将出错地址映射到的物理页面内容复制进去。内核会更改出错进程页表中相关的PTE，使其指向副本，并允许读写操作，然后在触发错误的指令处恢复出错进程。</p><p>由于PTE允许写入，重新执行的指令将不再触发错误。写时复制需要进行一些簿记工作，以帮助决定何时可以释放物理页面，因为每个页面可以被不同数量的页表引用，这取决于<code>fork</code>、页面错误、<code>exec</code>和<code>exit</code>的历史。</p><p>这种簿记允许一个重要优化：如果一个进程触发存储页面错误，而物理页面仅被该进程的页表引用，则无需复制。</p><p>写时复制使<code>fork</code>更快，因为<code>fork</code>无需复制内存。虽然有些内存最终需要在写入时复制，但通常大部分内存根本不需要复制。</p><p>一个常见例子是<code>fork</code>后紧跟<code>exec</code>：在<code>fork</code>后可能会写入几页内存，但随后子进程的<code>exec</code>会释放从父进程继承的大部分内存。</p><blockquote><p>子进程的 <code>exec</code> 系统调用用于加载并运行一个新的程序，替换掉当前进程的地址空间。</p></blockquote><p>写时复制<code>fork</code>消除了这种内存复制的需要。此外，写时复制<code>fork</code>是透明的：应用程序无需修改即可受益。</p><p>页表和页面错误的结合为写时复制之外的许多有趣可能性打开了大门。</p><h5 id="懒惰分配"><a href="#懒惰分配" class="headerlink" title="懒惰分配"></a>懒惰分配</h5><p>另一个广泛使用的特性是<strong>懒惰分配</strong>，它包含两部分。首先，当应用程序通过调用<code>sbrk</code>请求更多内存时，内核会记录大小的增加，但不会分配物理内存，也不会为新范围的虚拟地址创建PTE。其次，在这些新地址上的页面错误时，内核会分配一个物理页面并将其映射到页表中。与写时复制<code>fork</code>一样，内核可以对应用程序透明地实现懒惰分配。</p><blockquote><p>sbrk 是一个进程用来<strong>收缩或扩展其内存</strong>的系统调用。</p></blockquote><p>由于应用程序通常会请求比实际需要更多的内存，懒惰分配是一个胜利：<strong>内核完全不需要为应用程序从未使用的页面做任何工作</strong>。此外，如果应用程序请求大幅增加地址空间，那么没有懒惰分配的<code>sbrk</code>会很昂贵：如果应用程序请求1GB内存，内核需要分配并清零262,144个4096字节的页面。懒惰分配允许这种成本随时间分散。另一方面，懒惰分配会增加页面错误的额外开销，这涉及内核/用户切换。操作系统可以通过每次页面错误分配一批连续页面而不是一个页面，并为这些页面错误专门化内核入口/出口代码，从而降低这种成本。</p><h5 id="按需分页"><a href="#按需分页" class="headerlink" title="按需分页"></a>按需分页</h5><h6 id="1-按需分页的背景"><a href="#1-按需分页的背景" class="headerlink" title="1. 按需分页的背景"></a><strong>1. 按需分页的背景</strong></h6><p>在传统的程序加载方式中（如Xv6的<code>exec</code>实现），操作系统会在程序启动时将应用程序的所有文本段（代码）和数据段一次性加载到物理内存中。这种方式被称为“急切加载”（Eager Loading）。然而，这种方式存在一些问题：</p><ol><li><strong>启动成本高</strong>：如果应用程序较大，一次性从磁盘加载所有内容到内存会耗费大量时间，导致用户在启动程序时感受到明显的延迟。</li><li><strong>资源浪费</strong>：程序可能并不需要立即使用所有加载的页面，一次性加载所有内容会占用大量内存资源。</li></ol><hr><h6 id="2-按需分页的工作原理"><a href="#2-按需分页的工作原理" class="headerlink" title="2. 按需分页的工作原理"></a><strong>2. 按需分页的工作原理</strong></h6><p>为了优化程序加载过程，现代操作系统采用了“按需分页”技术。按需分页的核心思想是：<strong>只在真正需要时才加载页面内容</strong>。具体实现方式如下：</p><p><strong>2.1 创建页表并标记PTE为无效</strong></p><ol><li><strong>创建页表</strong>：操作系统为用户进程创建页表，但并不立即加载所有页面的内容。</li><li><strong>标记PTE为无效</strong>：在页表中，页面表项（PTE）被标记为无效（即页面不存在于内存中）。这意味着当进程访问这些页面时，会产生页面错误（Page Fault）。</li></ol><p><strong>2.2 页面错误处理</strong></p><p>当进程访问一个标记为无效的页面时，会触发页面错误。操作系统捕获页面错误并执行以下操作：</p><ol><li><strong>从磁盘加载页面</strong>：操作系统从磁盘读取所需的页面内容。</li><li><strong>更新页表</strong>：将对应的PTE标记为有效，并将页面映射到进程的用户地址空间。</li><li><strong>恢复执行</strong>：修复页面错误后，进程继续执行。</li></ol><hr><h6 id="3-按需分页的优点"><a href="#3-按需分页的优点" class="headerlink" title="3. 按需分页的优点"></a><strong>3. 按需分页的优点</strong></h6><ol><li><strong>提高响应速度</strong>：<ul><li>按需分页避免了一次性加载所有页面的开销，用户可以更快地看到程序的响应。</li><li>只有当进程真正访问某个页面时，才会触发页面错误并加载内容。</li></ul></li><li><strong>节省内存资源</strong>：<ul><li>程序可能永远不会用到某些页面，按需分页避免了不必要的内存占用。</li><li>提高了内存的利用率，支持运行更多或更大的程序。</li></ul></li><li><strong>透明性</strong>：<ul><li>按需分页对应用程序是透明的，应用程序无需修改即可受益于这一机制。</li><li>操作系统在后台处理页面错误和页面加载，应用程序无需感知这些细节。</li></ul></li></ol><hr><h6 id="4-与写时复制和懒惰分配的相似性"><a href="#4-与写时复制和懒惰分配的相似性" class="headerlink" title="4. 与写时复制和懒惰分配的相似性"></a><strong>4. 与写时复制和懒惰分配的相似性</strong></h6><p>按需分页与写时复制（Copy-On-Write，COW）和懒惰分配（Lazy Allocation）有相似之处：</p><ol><li><strong>写时复制（COW）</strong>：<ul><li>在<code>fork</code>操作中，子进程的页面最初与父进程共享。</li><li>只有当页面被写入时，操作系统才会复制页面内容。</li><li>这种机制减少了<code>fork</code>操作的开销，提高了效率。</li></ul></li><li><strong>懒惰分配（Lazy Allocation）</strong>：<ul><li>在内存分配时（如<code>malloc</code>），操作系统并不立即分配物理页面。</li><li>只有当进程访问分配的内存时，才会触发页面错误并分配物理页面。</li><li>这种机制避免了不必要的内存分配，节省了资源。</li></ul></li><li>按需分页<ul><li>本质上是因为如果一开始就将所有页都进行分配，如果这是一个小程序，可能会有大量的资源浪费</li></ul></li></ol><h3 id="4-7-现实世界中的应用"><a href="#4-7-现实世界中的应用" class="headerlink" title="4.7 现实世界中的应用"></a>4.7 现实世界中的应用</h3><p>跳板（trampoline）和陷阱帧（trapframe）可能看起来过于复杂。</p><p>一个主要的推动力是RISC-V在触发陷阱时尽可能少地执行操作，以实现非常快速的陷阱处理，这实际上非常重要。</p><p>因此，<strong>内核陷阱处理程序的最初几条指令实际上需要在用户环境中执行</strong>：使用用户页表和用户寄存器的内容。而且，陷阱处理程序最初并不知道一些有用的信息，例如正在运行的进程的身份或内核页表的地址。</p><p>解决方案之所以可能，是因为RISC-V提供了内核可以在进入用户空间之前存储信息的受保护位置：<code>sscratch</code>寄存器以及指向内核内存但因缺少<code>PTE_U</code>而受到保护的用户页表条目。Xv6的跳板和陷阱帧利用了这些RISC-V特性。</p><p>如果内核内存被映射到每个进程的用户页表中（并设置适当的PTE权限标志），那么就可以消除对特殊跳板页的需求。这也将消除从用户空间进入内核时的页表切换需求。这反过来又允许内核中的系统调用实现利用当前进程的用户内存被映射的事实，从而允许内核代码直接解引用用户指针。许多操作系统已经使用这些想法来提高效率。Xv6避免采用这些方法，以减少由于意外使用用户指针而导致内核中安全漏洞的可能性，并减少一些必要的复杂性，以确保用户和内核虚拟地址不重叠。</p><p>生产级操作系统实现了写时复制<code>fork</code>、懒惰分配、按需分页、磁盘分页、内存映射文件等功能。此外，生产级操作系统会尽量使用所有的物理内存，无论是用于应用程序还是缓存（例如文件系统的缓冲区缓存，我们将在第8.2节中讨论）。在这方面，Xv6显得非常天真：你希望你的操作系统能够使用你购买的物理内存，但Xv6并没有做到。此外，如果Xv6耗尽了内存，它会向正在运行的应用程序返回错误或将其杀死，而不是例如驱逐另一个应用程序的页面。</p>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 操作系统 </tag>
            
            <tag> MIT6.s081 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Calling Convention</title>
      <link href="/2025/02/17/cao-zuo-xi-tong-diao-yong-xie-yi/"/>
      <url>/2025/02/17/cao-zuo-xi-tong-diao-yong-xie-yi/</url>
      
        <content type="html"><![CDATA[<p>本章介绍了RV32和RV64程序的C编译器标准以及两种调用约定：一种是针对基础ISA加上标准通用扩展（RV32G/RV64G）的调用约定，另一种是针对缺少浮点单元的实现（例如RV32I/RV64I）的软浮点调用约定。具有ISA扩展的实现可能需要扩展的调用约定。</p><h3 id="重要要点"><a href="#重要要点" class="headerlink" title="重要要点"></a><strong>重要要点</strong></h3><ol><li><strong>C数据类型和对齐（18.1）</strong>：<ul><li><code>int</code> 在RV32和RV64下都是32位宽。</li><li><code>long</code> 和指针在RV32下是32位，在RV64下是64位。</li><li><code>long long</code> 是64位，<code>float</code> 是32位，<code>double</code>是64位，<code>long double</code>是128位。</li><li>所有数据类型在内存中保持自然对齐。</li></ul></li><li><strong>RVG调用约定（18.2）</strong>：<ul><li>使用寄存器<code>a0-a7</code>传递最多8个整数参数，<code>fa0-fa7</code>传递最多8个浮点参数。</li><li>较小的参数放在参数寄存器的低位，并在栈上是字节对齐的。</li><li>大于指针宽度两倍的参数按引用传递。</li><li>返回值通过<code>a0/hp0</code>、<code>a1/hp1</code>返回，浮点值用<code>fa0/fa1</code>。</li><li>栈向下增长，<code>sp</code>总是保持16字节对齐。</li></ul></li><li><strong>软浮点调用约定（18.3）</strong>：<ul><li>用于无浮点硬件的实现，浮点参数和返回值通过整数寄存器传递。</li><li>在RV32下，<code>double</code>参数传递在<code>a2-a3</code>中，<code>long double</code>通过引用传递。</li></ul></li></ol><h3 id="关键概念和名词"><a href="#关键概念和名词" class="headerlink" title="关键概念和名词"></a><strong>关键概念和名词</strong></h3><ul><li><strong>RVG调用约定</strong>：用于标准ISA扩展（RV32G/RV64G）的参数和返回值传递规则。</li><li><strong>软浮点调用约定</strong>：用于无浮点硬件实现的调用规则，避免使用浮点指令。</li><li><strong>参数传递规则</strong>：参数通过寄存器（整数和浮点）或栈传递，取决于参数数目和类型。</li><li><strong>返回值传递</strong>：简单类型在寄存器返回，复杂结构返回指针。</li><li><strong>寄存器角色</strong>：指明哪些寄存器由调用者保存，哪些由被调用者保存。</li><li><strong>ISA</strong>:（Instruction Set Architecture，指令集架构）是指计算机硬件与软件之间的接口规范，定义了处理器能够理解和执行的指令集。它主要包括以下几个关键方面：<ul><li><strong>指令集</strong>：定义了处理器能够执行的所有指令，包括操作码（指示操作类型）和操作数（操作对象）。</li><li><strong>寻址模式</strong>：定义了如何访问操作数，例如立即寻址、寄存器寻址、直接寻址等。</li><li><strong>数据类型</strong>：定义了处理器支持的数据类型，如整数、浮点数等。</li><li><strong>寄存器</strong>：定义了处理器的寄存器数量、类型和用途。</li><li><strong>中断和异常处理</strong>：定义了处理器如何处理中断和异常。</li><li><strong>特权指令</strong>：定义了只有操作系统才能执行的指令，用于保护系统资源。</li></ul></li></ul><h3 id="需要了解的背景知识"><a href="#需要了解的背景知识" class="headerlink" title="需要了解的背景知识"></a><strong>需要了解的背景知识</strong></h3><ul><li><strong>寄存器和内存布局</strong>：理解整数和浮点寄存器的分配和使用。</li><li><strong>调用堆栈机制</strong>：清楚栈增长方向和栈帧结构。</li><li><strong>RISC-V架构基础</strong>：了解RISC-V基本指令集和ISA扩展。</li><li><strong>C语言数据类型</strong>：熟悉C中数据类型（如<code>int</code>, <code>long</code>等）的大小和对齐。</li></ul><h3 id="学习目标"><a href="#学习目标" class="headerlink" title="学习目标"></a><strong>学习目标</strong></h3><ul><li><strong>理解参数和返回值传递规则</strong>：掌握参数如何在寄存器或栈上传递，以及返回值的传递方式。</li><li><strong>掌握寄存器使用和保存规则</strong>：明确哪个寄存器是调用者保存，哪个是被调用者保存。</li><li><strong>熟悉栈的使用</strong>：了解栈如何存储未在寄存器传递的参数和局部变量。</li><li><strong>比较RVG和软浮点调用约定</strong>：知道两种调用约定的不同适用场景和差异。</li></ul><h3 id="重要代码和函数"><a href="#重要代码和函数" class="headerlink" title="重要代码和函数"></a><strong>重要代码和函数</strong></h3><ul><li><strong>参数传递逻辑</strong>：在编译器或汇编代码中，函数调用时参数的入栈或寄存器分配部分。</li><li><strong>函数返回值处理</strong>：如何从寄存器或内存读取函数返回值的代码部分。</li><li><strong>栈帧管理</strong>：函数调用时，栈指针调整、局部变量分配等代码片段。</li></ul><h3 id="学习建议"><a href="#学习建议" class="headerlink" title="学习建议"></a><strong>学习建议</strong></h3><ul><li><strong>阅读《RISC-V ISA手册》</strong>：特别是有关用户级ISA的部分，理解寄存器和指令。</li><li><strong>研究编译器生成的汇编代码</strong>：查看C代码编译后如何应用调用约定。</li><li><strong>动手实验</strong>：在支持RISC-V的环境中编写代码，观察参数传递和返回值的处理。</li><li><strong>强化基础</strong>：充分理解寄存器、栈、C数据类型等基础知识，以便更好地掌握调用约定。</li></ul><h3 id="18-1-C数据类型和对齐"><a href="#18-1-C数据类型和对齐" class="headerlink" title="18.1 C数据类型和对齐"></a>18.1 C数据类型和对齐</h3><h5 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h5><ul><li><strong>整数类型</strong>：<ul><li><code>int</code>：32位宽。</li><li><code>long</code> 和 <strong>指针</strong>：<ul><li>RV32：32位宽（ILP32模型）。</li><li>RV64：64位宽（LP64模型）。</li></ul></li><li><code>long long</code>：64位宽。</li><li><code>char</code> 和 <code>unsigned char</code>：8位宽，存储时零扩展。</li><li><code>unsigned short</code>：16位宽，存储时零扩展。</li><li><code>signed char</code> 和 <code>short</code>：分别8位和16位宽，存储时符号扩展。</li></ul></li><li><strong>浮点类型</strong>：<ul><li><code>float</code>：32位IEEE 754-2008浮点数。</li><li><code>double</code>：64位IEEE 754-2008浮点数。</li><li><code>long double</code>：128位IEEE浮点数。</li></ul></li><li><strong>特殊规则</strong>：<ul><li>在RV64中，32位类型（如<code>int</code>）存储在寄存器中时，会以符号扩展形式存储其32位值（即使是无符号类型）。</li></ul></li></ul><h5 id="数据对齐"><a href="#数据对齐" class="headerlink" title="数据对齐"></a>数据对齐</h5><ul><li>RV32和RV64的C编译器以及兼容软件在将数据类型存储到内存时会保持<strong>自然对齐</strong>。这意味着数据类型会按照其大小对齐到相应的内存地址，以提高访问效率。<ul><li>*<em>数据对齐会实现访问效率的提升，这是我们在page table一节也提到过的内容。不太正确地来将，我们所使用指针加减动作，其实每次都是实现的一个某一个量上的变化，是固定的，比如int <em>a,则 a + 1,实际上是移动了4个字节，因为a的类型是4个字节，这是一种统一性</em></em></li></ul></li></ul><p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250218210026952.png" alt="RISC-V C程序所支持的本地数据类型"></p><h3 id="18-2-RVG调用约定"><a href="#18-2-RVG调用约定" class="headerlink" title="18.2 RVG调用约定"></a>18.2 RVG调用约定</h3><p><code>在这里，我们先要思考要给问题，calling convention为什么被设计出来。在汇编语言中，如果一个函数需要调用另一个函数，是需要保留上下文和现场的，方便后面的现场恢复。这也就引生出来了保存寄存器(save register)和一般寄存器。我们一般认为保存寄存器中的内容，是绝对可信的，因为在calle或者caller，二者必有一人会保存信息在其中，并且在某个阶段恢复它们（根据协议来看）。但是一般寄存器是绝对不能相信的。</code></p><p>所以我们可以得出结论：</p><ul><li>calling convention是通过使用寄存器实现数据传递的。</li><li>协议定义的是栈的使用和寄存器的保存规则</li></ul><hr><h4 id="1-参数传递规则"><a href="#1-参数传递规则" class="headerlink" title="1. 参数传递规则"></a><strong>1. 参数传递规则</strong></h4><ol><li><p><strong>寄存器传递</strong>：</p><ul><li>最多使用8个整数寄存器（<code>a0–a7</code>）和8个浮点寄存器（<code>fa0–fa7</code>）来传递参数。</li><li>参数被视为结构体字段，前八个指针大小的字段通过寄存器传递。</li><li>浮点参数通过浮点寄存器传递，除非它们是联合体或结构体数组字段的一部分，此时通过整数寄存器传递。</li><li>可变参数函数中的浮点参数也通过整数寄存器传递。</li></ul></li><li><h4 id="小端字节序和对齐："><a href="#小端字节序和对齐：" class="headerlink" title="小端字节序和对齐："></a><strong>小端字节序和对齐</strong>：</h4><ul><li>小于指针大小的参数通过寄存器的最低有效位传递。</li><li>子指针大小的参数通过栈传递时，存储在指针大小字段的低地址部分。</li><li>参数大小为指针大小的两倍时：<ul><li>通过栈传递时自然对齐。</li><li>通过寄存器传递时，存储在对齐的偶-奇寄存器对中，偶数寄存器存储最低有效位（例如，<code>long long</code>在RV32中通过<code>a2</code>和<code>a3</code>传递）。</li></ul></li></ul></li><li><p><strong>大参数传递</strong>：</p><ul><li>大于指针大小两倍的参数通过引用传递。</li><li>结构体中未通过寄存器传递的部分通过栈传递，栈指针<code>sp</code>指向第一个未通过寄存器传递的参数。</li></ul></li></ol><hr><h4 id="2-返回值传递规则"><a href="#2-返回值传递规则" class="headerlink" title="2. 返回值传递规则"></a><strong>2. 返回值传递规则</strong></h4><ol><li>返回值通过以下寄存器传递：<ul><li>整数返回值：<code>a0</code>和<code>a1</code>。</li><li>浮点返回值：<ul><li>如果是原始类型或仅包含一到两个浮点值的结构体成员，通过<code>fa0</code>和<code>fa1</code>返回。</li><li>其他能够放入两个指针大小的返回值通过<code>a0</code>和<code>a1</code>返回。</li></ul></li><li>更大的返回值通过内存传递，调用者分配内存区域，并将其指针作为隐式第一个参数传递给被调用者。</li></ul></li></ol><hr><h4 id="3-栈和寄存器的使用规则"><a href="#3-栈和寄存器的使用规则" class="headerlink" title="3. 栈和寄存器的使用规则"></a><strong>3. 栈和寄存器的使用规则</strong></h4><ol><li><strong>栈</strong>：<ul><li>栈向下增长，栈指针<code>sp</code>始终保持16字节对齐。</li></ul></li><li><strong>寄存器分类</strong>：<ul><li><strong>参数寄存器</strong>：<ul><li>整数寄存器：<code>a0–a7</code>。</li><li>浮点寄存器：<code>fa0–fa7</code>。</li></ul></li><li><strong>临时寄存器（易失性）</strong>：<ul><li>整数寄存器：<code>t0–t6</code>。</li><li>浮点寄存器：<code>ft0–ft11</code>。</li><li>如果后续需要使用，必须由调用者保存。</li></ul></li><li><strong>保留寄存器（非易失性）</strong>：<ul><li>整数寄存器：<code>s0–s11</code>。</li><li>浮点寄存器：<code>fs0–fs11</code>。</li><li>如果使用了这些寄存器，必须由被调用者保存。</li></ul></li></ul></li></ol><hr><h4 id="4-总结"><a href="#4-总结" class="headerlink" title="4. 总结"></a><strong>4. 总结</strong></h4><p>RVG调用约定通过寄存器高效地传递参数和返回值，同时定义了栈的使用和寄存器的保存规则。这种调用约定旨在优化函数调用的性能，同时保持代码的可移植性和一致性。</p><p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250218210649562.png" alt="RISC-V calling convention register usage."></p><h3 id="18-3-软浮点调用约定"><a href="#18-3-软浮点调用约定" class="headerlink" title="18.3 软浮点调用约定"></a>18.3 软浮点调用约定</h3><h5 id="核心内容总结"><a href="#核心内容总结" class="headerlink" title="核心内容总结"></a><strong>核心内容总结</strong></h5><p>软浮点调用约定是<strong>针对缺乏浮点硬件</strong>的RISC-V实现（如RV32I或RV64I）设计的调用约定。它完全避免使用浮点寄存器和浮点指令，而是通过整数寄存器来传递和处理浮点参数。以下是关键要点的总结：</p><hr><h5 id="1-浮点参数的处理"><a href="#1-浮点参数的处理" class="headerlink" title="1. 浮点参数的处理"></a><strong>1. 浮点参数的处理</strong></h5><ol><li><strong>浮点参数传递</strong>：<ul><li>浮点参数通过<strong>整数寄存器</strong>传递，遵循与相同大小的整数参数相同的规则。</li><li><strong>RV32</strong>：<ul><li>单精度浮点（<code>float</code>）：通过单个整数寄存器传递。</li><li>双精度浮点（<code>double</code>）：通过两个连续的整数寄存器传递（例如，<code>a2</code>和<code>a3</code>）。</li><li>扩展精度浮点（<code>long double</code>）：通过引用传递（存储在内存中，传递指针）。</li></ul></li><li><strong>RV64</strong>：<ul><li>单精度浮点（<code>float</code>）和双精度浮点（<code>double</code>）：通过单个整数寄存器传递。</li><li>扩展精度浮点（<code>long double</code>）：通过引用传递。</li></ul></li></ul></li><li><strong>返回值</strong>：<ul><li>浮点返回值通过整数寄存器返回。</li><li><strong>RV32</strong>：<ul><li>单精度浮点：通过<code>a0</code>返回。</li><li>双精度浮点：通过<code>a0</code>和<code>a1</code>返回。</li></ul></li><li><strong>RV64</strong>：<ul><li>单精度浮点和双精度浮点：通过<code>a0</code>返回。</li></ul></li></ul></li><li><strong>示例</strong>：<ul><li>在RV32中，函数<code>double foo(int, double, long double)</code>：<ul><li>第一个参数（<code>int</code>）通过<code>a0</code>传递。</li><li>第二个参数（<code>double</code>）通过<code>a2</code>和<code>a3</code>传递。</li><li>第三个参数（<code>long double</code>）通过<code>a4</code>以引用方式传递。</li><li>返回值（<code>double</code>）通过<code>a0</code>和<code>a1</code>返回。</li></ul></li><li>在RV64中，参数通过<code>a0</code>、<code>a1</code>和<code>a2</code>-<code>a3</code>对传递，返回值通过<code>a0</code>返回。</li></ul></li></ol><hr><h5 id="2-动态舍入模式和异常处理"><a href="#2-动态舍入模式和异常处理" class="headerlink" title="2. 动态舍入模式和异常处理"></a><strong>2. 动态舍入模式和异常处理</strong></h5><ul><li>动态舍入模式和累积异常标志可以通过C99标准头文件<code>&lt;fenv.h&gt;</code>提供的例程访问。</li><li>这些功能在软浮点实现中通过软件模拟完成，因为硬件不支持浮点操作。</li></ul><hr><h5 id="3-总结"><a href="#3-总结" class="headerlink" title="3. 总结"></a><strong>3. 总结</strong></h5><p>软浮点调用约定是为缺乏浮点硬件的RISC-V实现设计的，通过整数寄存器传递和处理浮点参数。这种调用约定确保了即使在没有浮点单元的情况下，浮点运算也能通过软件模拟完成，同时保持与RVG调用约定的兼容性。</p>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 操作系统 </tag>
            
            <tag> MIT6.s081 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Lab3 page tables</title>
      <link href="/2025/02/17/lab3-page-tables/"/>
      <url>/2025/02/17/lab3-page-tables/</url>
      
        <content type="html"><![CDATA[<h2 id="Speed-up-system-calls-easy"><a href="#Speed-up-system-calls-easy" class="headerlink" title="Speed up system calls (easy)"></a>Speed up system calls (<a href="https://pdos.csail.mit.edu/6.S081/2021/labs/guidance.html">easy</a>)</h2><h4 id="题目"><a href="#题目" class="headerlink" title="题目"></a>题目</h4><p>一些操作系统（例如 Linux）通过在<strong>用户空间和内核之间共享一个只读区域</strong>的数据来加速某些系统调用。这消除了在执行这些系统调用时需要跨越内核边界的需要。为了帮助你学习如何将映射插入页表，你的第一个任务是在 xv6 中为 <code>getpid()</code> 系统调用实现这种优化。</p><p>当每个进程被创建时，在 <strong>USYSCALL（在 memlayout.h 中定义的一个虚拟地址）处映射一个只读页面</strong>。在该页面的<strong>起始位置</strong>存储一个 struct usyscall（也在 memlayout.h 中定义），并将其初始化为存储当前进程的 PID。在本实验中，用户空间一侧已经提供了 ugetpid()，它将自动使用 USYSCALL 映射。如果在运行 pgtbltest 时 ugetpid 测试用例通过，你将获得这部分实验的全部分数。</p><p>以下是一些提示：</p><ul><li>你可以在 <code>proc_pagetable()</code>（位于 kernel/proc.c）中执行映射。</li><li>选择权限位，只允许用户空间读取该页面。</li><li>你可能会发现 <code>mappages()</code> 是一个有用的工具。</li><li>不要忘记在 <code>allocproc()</code> 中分配和初始化该页面。</li><li>确保在 <code>freeproc()</code> 中释放该页面。</li></ul><p><strong>思考问题：</strong><br>xv6 中还有哪些其他系统调用可以通过这种共享页面的方式加速？解释如何实现。</p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250219155648287.png" alt="syscall在" style="zoom:67%;"><h4 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h4><ul><li>因为每个进程都需要存储一个usyscall，所以我们应该在<code>proc.h</code>，在结构体上加入。</li></ul><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Per-process state</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> {</span></span><br><span class="line">  <span class="type">pagetable_t</span> pagetable;       <span class="comment">// User page table</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">trapframe</span> *<span class="title">trapframe</span>;</span> <span class="comment">// data page for trampoline.S</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">usyscall</span> *<span class="title">usyscall</span>;</span>   <span class="comment">// 指向用户态系统调用页面的指针</span></span><br><span class="line">};</span><br><span class="line"></span><br></pre></td></tr></tbody></table></figure><ul><li>使用mappages来实现页面映射</li></ul><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">  <span class="comment">// map 一个只读页面USYSCALL</span></span><br><span class="line">  <span class="keyword">if</span>(mappages(pagetable, USYSCALL, PGSIZE,</span><br><span class="line">              (uint64)(p-&gt;usyscall), PTE_R | PTE_U) &lt; <span class="number">0</span>){</span><br><span class="line">    uvmunmap(pagetable, TRAMPOLINE, <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    uvmunmap(pagetable, TRAPFRAME, <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    uvmfree(pagetable, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  }</span><br><span class="line"></span><br><span class="line">mappages(<span class="type">pagetable_t</span> pagetable, uint64 va, uint64 size, uint64 pa, <span class="type">int</span> perm)</span><br></pre></td></tr></tbody></table></figure><p><strong>特别注意：VA虚拟地址是定义一个位置。而pa，物理地址实际上是proc.h中定义的。</strong></p><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// static struct proc* allocproc(void)</span></span><br><span class="line"><span class="keyword">if</span>((p-&gt;usyscall = (<span class="keyword">struct</span> usyscall *)kalloc()) == <span class="number">0</span>){</span><br><span class="line">  freeproc(p);</span><br><span class="line">  release(&amp;p-&gt;lock);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">}</span><br><span class="line">p-&gt;usyscall-&gt;pid = p-&gt;pid; <span class="comment">//store pid</span></span><br></pre></td></tr></tbody></table></figure><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>(p-&gt;usyscall)</span><br><span class="line">  kfree((<span class="type">void</span>*)p-&gt;usyscall);</span><br><span class="line">p-&gt;usyscall = <span class="number">0</span>;</span><br></pre></td></tr></tbody></table></figure><h3 id="打印页表（简单）"><a href="#打印页表（简单）" class="headerlink" title="打印页表（简单）"></a>打印页表（简单）</h3><p>为了帮助你可视化RISC-V页表，并可能为未来的调试提供帮助，你的第二个任务是编写一个打印页表内容的函数。</p><p>定义一个名为<code>vmprint()</code>的函数。它应该接受一个<code>pagetable_t</code>类型的参数，并按照下面描述的格式打印该页表。在<code>exec.c</code>中，在<code>return argc</code>之前插入<code>if (p-&gt;pid == 1) vmprint(p-&gt;pagetable)</code>，以打印第一个进程的页表。如果你通过了<code>make grade</code>中的<code>pte</code>打印测试，你将获得这部分实验的全部分数。</p><p>现在，当你启动Xv6时，它应该会打印类似以下内容，<strong>描述第一个进程</strong>在完成<code>exec()</code>初始化时的页表：</p><p>复制</p><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">page table <span class="number">0x0000000087f6e000</span></span><br><span class="line"> ..<span class="number">0</span>: pte <span class="number">0x0000000021fda801</span> pa <span class="number">0x0000000087f6a000</span></span><br><span class="line"> .. ..<span class="number">0</span>: pte <span class="number">0x0000000021fda401</span> pa <span class="number">0x0000000087f69000</span></span><br><span class="line"> .. .. ..<span class="number">0</span>: pte <span class="number">0x0000000021fdac1f</span> pa <span class="number">0x0000000087f6b000</span></span><br><span class="line"> .. .. ..<span class="number">1</span>: pte <span class="number">0x0000000021fda00f</span> pa <span class="number">0x0000000087f68000</span></span><br><span class="line"> .. .. ..<span class="number">2</span>: pte <span class="number">0x0000000021fd9c1f</span> pa <span class="number">0x0000000087f67000</span></span><br><span class="line"> ..<span class="number">255</span>: pte <span class="number">0x0000000021fdb401</span> pa <span class="number">0x0000000087f6d000</span></span><br><span class="line"> .. ..<span class="number">511</span>: pte <span class="number">0x0000000021fdb001</span> pa <span class="number">0x0000000087f6c000</span></span><br><span class="line"> .. .. ..<span class="number">509</span>: pte <span class="number">0x0000000021fdd813</span> pa <span class="number">0x0000000087f76000</span></span><br><span class="line"> .. .. ..<span class="number">510</span>: pte <span class="number">0x0000000021fddc07</span> pa <span class="number">0x0000000087f77000</span></span><br><span class="line"> .. .. ..<span class="number">511</span>: pte <span class="number">0x0000000020001c0b</span> pa <span class="number">0x0000000080007000</span></span><br></pre></td></tr></tbody></table></figure><p>第一行显示传递给<code>vmprint</code>的参数。之后，每一行对应一个PTE（页表条目），包括引用树中更深层次的页表页面的PTE。每个PTE行通过缩进的“..”数量表示其在树中的深度。每个PTE行显示其在页表页面中的索引、PTE的位以及从PTE中提取的物理地址。不要打印无效的PTE。在上面的例子中，顶级页表页面有条目0和255的映射。条目0的下一层只有索引0被映射，而该索引0的底层有条目0、1和2被映射。</p><p>你的代码可能会输出与上面不同的物理地址。条目数量和虚拟地址应该是相同的。</p><h4 id="一些建议："><a href="#一些建议：" class="headerlink" title="一些建议："></a>一些建议：</h4><ul><li>你可以将<code>vmprint()</code>放在<code>kernel/vm.c</code>中。</li><li>使用<code>kernel/riscv.h</code>文件末尾的宏。</li><li><code>freewalk</code>函数可能会给你一些启发。</li><li>在<code>kernel/defs.h</code>中定义<code>vmprint</code>的原型，以便从<code>exec.c</code>中调用它。</li><li>在<code>printf</code>调用中使用<code>%p</code>打印完整的64位十六进制PTE和地址，如示例所示。</li></ul><h4 id="解释vmprint的输出"><a href="#解释vmprint的输出" class="headerlink" title="解释vmprint的输出"></a>解释<code>vmprint</code>的输出</h4><p>根据文本中的图3-4解释<code>vmprint</code>的输出。页0中包含什么？页2中有什么？在用户模式下运行时，进程能否读写页1映射的内存？倒数第三页包含什么？</p><hr><h4 id="解释示例输出"><a href="#解释示例输出" class="headerlink" title="解释示例输出"></a>解释示例输出</h4><ol><li><strong>页表的结构</strong>：<ul><li>页表是一个多级结构，每一级由页表条目（PTE）组成。</li><li>每个PTE可以指向一个物理页面或下一级页表。</li></ul></li><li><strong>页0的内容</strong>：<ul><li>页0通常包含内核代码或数据。在Xv6中，页0可能映射了内核的某些部分。</li></ul></li><li><strong>页2的内容</strong>：<ul><li>页2可能包含用户空间的堆或栈。具体取决于进程的内存布局。</li></ul></li><li><strong>页1的访问权限</strong>：<ul><li>在用户模式下，进程是否可以读写页1映射的内存取决于PTE的权限位（如<code>PTE_U</code>和<code>PTE_W</code>）。</li><li>如果PTE的<code>PTE_U</code>位被设置，则用户模式可以访问该页面；如果<code>PTE_W</code>位被设置，则可以写入。</li></ul></li><li><strong>倒数第三页的内容</strong>：<ul><li>倒数第三页可能是一个用户空间的页面，用于存储进程的代码、数据或栈。</li><li>具体内容取决于进程的内存布局和页表的映射。</li></ul></li></ol><p>通过<code>vmprint</code>的输出，你可以清晰地看到进程的内存布局以及每个页面的映射情况。</p><h4 id="思考-1"><a href="#思考-1" class="headerlink" title="思考"></a>思考</h4><p>在分页机制中，页表项（PTE）可以有两种类型：</p><ul><li><strong>叶子页表项（Leaf PTE）</strong>：直接映射到物理页面（通常是 4KB 的内存块）。</li><li><strong>非叶子页表项（Non-Leaf PTE）</strong>：指向下一个级别的页表。</li></ul><p>注意如何判断一个PTE是否是叶子表项，也就是根据flags进行判断</p><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 如果页表项有效（PTE_V），并且它指向下一级页表（无 PTE_R/W/X 权限位）。</span></span><br><span class="line"><span class="keyword">if</span> ((pte &amp; PTE_V) &amp;&amp; (pte &amp; (PTE_R | PTE_W | PTE_X)) == <span class="number">0</span>)</span><br></pre></td></tr></tbody></table></figure><p><strong>该题的要点，在于引导我们思考，虚拟地址到物理地址的传播路径</strong></p><h3 id="检测哪些页面已被访问（难度较大）"><a href="#检测哪些页面已被访问（难度较大）" class="headerlink" title="检测哪些页面已被访问（难度较大）"></a>检测哪些页面已被访问（难度较大）</h3><p>一些垃圾回收器（一种自动内存管理的形式）可以从关于哪些页面已被访问（读或写）的信息中受益。在本实验的这一部分，你需要为 xv6 添加一个新功能，通过检查 RISC-V 页表中的访问位，将这些信息报告给用户空间。当 RISC-V 硬件页表解析器解决 TLB 缺失时，它会在 PTE 中标记这些位。</p><p>你的任务是实现 <code>pgaccess()</code>，这是一个系统调用，用于报告哪些页面已被访问。该系统调用接受三个参数。首先，它接受要检查的第一个用户页面的起始虚拟地址。其次，它接受要检查的页面数量。最后，它接受一个用户地址，用于存储结果到一个位掩码（一种数据结构，每个页面使用一个位，其中第一个页面对应最低有效位）。如果在运行 <code>pgtbltest</code> 时，<code>pgaccess</code> 测试用例通过，你将获得本实验部分的全部分数。</p><p>以下是一些提示：</p><ul><li>从在 <code>kernel/sysproc.c</code> 中实现 <code>sys_pgaccess()</code> 开始。</li><li>你需要使用 <code>argaddr()</code> 和 <code>argint()</code> 解析参数。</li><li>对于输出的位掩码，将临时缓冲区存储在内核中并在填充正确的位后将其复制到用户空间（通过 <code>copyout()</code>）会更方便。</li><li>设置可扫描页面数量的上限是可以接受的。</li><li><code>kernel/vm.c</code> 中的 <code>walk()</code> 函数对于找到正确的 PTE 非常有用。</li><li>你需要在 <code>kernel/riscv.h</code> 中定义访问位 <code>PTE_A</code>。请查阅 RISC-V 手册以确定其值。</li><li>在检查后务必清除 <code>PTE_A</code>。否则，将无法确定自上次调用 <code>pgaccess()</code> 以来页面是否被访问（即，该位将永远被设置）。</li><li><code>vmprint()</code> 可能有助于调试页表。</li></ul>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 操作系统 </tag>
            
            <tag> MIT6.s081 </tag>
            
            <tag> lab </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Chapter 3 页表与内存管理</title>
      <link href="/2025/02/17/cao-zuo-xi-tong-lec3/"/>
      <url>/2025/02/17/cao-zuo-xi-tong-lec3/</url>
      
        <content type="html"><![CDATA[<p><strong>Chapter 3 Page tables</strong></p><hr><h3 id="重要要点"><a href="#重要要点" class="headerlink" title="重要要点"></a><strong>重要要点</strong></h3><ul><li><strong>页表与内存管理</strong>：<ul><li><strong>分页机制实现了虚拟地址到物理地址的转换</strong>，每个进程拥有独立的地址空间。</li><li>页表是内存管理的关键<strong>数据结构</strong>，用于<strong>记录虚拟地址与物理地址的映射关系</strong>。</li><li>操作系统通过<strong>页表实现内存保护和资源隔离</strong>，确保进程不会访问其他进程或内核的内存。</li><li>页面错误（Page Fault）是由<strong>分页硬件在无法找到有效 PTE 时触发的异常</strong>，操作系统可以利用软件实现的页错误处理程序来管理系统内存。</li></ul></li><li><strong>地址空间布局</strong>：<ul><li>进程的用户地址空间包括文本、数据、堆栈和堆，内核地址空间独立于用户地址空间，包含内核代码、数据和内核堆栈。</li><li>xv6 的内核地址空间和用户地址空间都通过页表进行映射，确保内核和用户代码使用不同的虚拟地址范围。</li></ul></li><li><strong>硬件支持与功能实现</strong>：<ul><li>RISC-V 架构提供了硬件分页支持，包括页表结构（页目录和页表）以及相关的控制寄存器（如 <code>satp</code>）。</li><li>xv6 利用硬件分页机制实现了高效的内存管理，包括地址翻译、内存保护和页面错误处理。</li></ul></li></ul><hr><h3 id="关键概念和名词"><a href="#关键概念和名词" class="headerlink" title="关键概念和名词"></a><strong>关键概念和名词</strong></h3><ol><li><strong>页表（Page Table）</strong>：<ul><li>用于实现虚拟地址到物理地址转换的数据结构，由操作系统维护和更新。</li><li>在 xv6 中，每个进程的页表保存在 <code>struct proc</code> 中，内核页表是全局的，保存在 <code>kern_pgdir</code> 中。</li></ul></li><li><strong>调度算法（Scheduling Algorithm）</strong>：<ul><li>虽然主要在 Chapter7 中详细讨论，但页表管理的效率对调度算法至关重要，因为进程切换和时间片轮转需要保存和恢复进程的页表。</li></ul></li><li><strong>fork()</strong>：<ul><li>xv6 中实现的 <code>fork</code> 系统调用通过复制父进程的页表来创建子进程，确保子进程拥有与父进程相同的内存布局。</li></ul></li><li><strong>虚存访问模型（Virtual Memory Access Model）</strong>：<ul><li>包括分页、段式和段页式等多种方式，xv6 采用分页模型，硬件和软件协同工作，实现高效内存访问和管理。</li></ul></li><li><strong>内存映射（Memory Mapping）</strong>：<ul><li>将文件或设备映射到进程的地址空间，操作系统通过修改页表实现，允许进程像访问普通内存一样访问文件或设备。</li></ul></li></ol><hr><h3 id="需要了解的背景知识"><a href="#需要了解的背景知识" class="headerlink" title="需要了解的背景知识"></a><strong>需要了解的背景知识</strong></h3><ul><li><strong>操作系统原理</strong>：<ul><li>内存管理原理，包括虚拟内存、物理内存、页面错误和内存映射等概念。</li><li>进程管理，包括进程隔离、上下文切换和虚拟地址空间分配。</li><li>文件系统知识，包括文件存储和访问方式。</li><li>虚存访问模型，了解操作系统如何管理内存和地址空间。</li></ul></li><li><strong>数据结构</strong>：<ul><li>理解队列、堆栈、链表等数据结构，因为页表和其他内存管理结构通常使用这些数据结构实现。</li><li>了解树形结构，因为页表可以被视为多层树结构。</li></ul></li><li><strong>系统编程</strong>：<ul><li>熟悉 C 语言和汇编语言编程，因为 xv6 是用 C 语言实现的，并包含一些汇编代码。</li><li>了解操作系统内核编程的基本概念，如中断、异常处理和系统调用。</li></ul></li></ul><hr><h3 id="学习目标"><a href="#学习目标" class="headerlink" title="学习目标"></a><strong>学习目标</strong></h3><ol><li><strong>理解虚存访问模型和分页机制</strong>：<ul><li>掌握虚拟地址到物理地址的翻译过程，以及页表在这一过程中的作用。</li><li>弄清楚页目录和页表的结构如何构建分页模型。</li></ul></li><li><strong>掌握内存管理的实现细节</strong>：<ul><li>理解 xv6 的内存管理系统如何分配和回收内存。</li><li>了解页面错误的处理机制，包括如何为新页表项分配物理内存。</li></ul></li><li><strong>分析和理解重要代码和函数</strong>：<ul><li>能够阅读和理解 xv6 中与页表相关的源码，如 <code>walk</code>、<code>mappages</code> 等函数的实现。</li><li>理解如何通过修改页表实现内存映射和共享。</li></ul></li><li><strong>学习硬件与软件的协同工作</strong>：<ul><li>知道 RISC-V 架构中与分页相关的硬件特性，如 <code>satp</code> 寄存器、页面大小和分页级别。</li><li>理解操作系统如何利用这些硬件特性实现高效的内存管理。</li></ul></li></ol><hr><h3 id="重要代码和函数"><a href="#重要代码和函数" class="headerlink" title="重要代码和函数"></a><strong>重要代码和函数</strong></h3><ol><li><p><strong><code>walk</code> 函数</strong>：</p><p>c复制</p><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">pte_t</span> *<span class="title function_">walk</span><span class="params">(<span class="type">pagetable_t</span> pagetable, uint64 va, <span class="type">int</span> alloc)</span> {</span><br><span class="line">    <span class="type">pte_t</span> *pte;</span><br><span class="line">    uint64 pfn;</span><br><span class="line"></span><br><span class="line">    pte = &amp;pagetable[PDX(va)];</span><br><span class="line">    <span class="keyword">if</span> (*pte &amp; PTE_V) {</span><br><span class="line">        pfn = PTE2PA(*pte) / PGSIZE;</span><br><span class="line">        pte = (<span class="type">pte_t</span>*)kvmalloc(pfn) + PTX(va);</span><br><span class="line">    } <span class="keyword">else</span> <span class="keyword">if</span> (alloc) {</span><br><span class="line">        <span class="comment">// 分配新的页表页并映射</span></span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> pte;</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><ul><li><strong>作用</strong>：查找虚拟地址对应的页表项，并根据需要分配新的页表页。</li></ul></li><li><p><strong><code>mappages</code> 函数</strong>：</p><p>c复制</p><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">mappages</span><span class="params">(<span class="type">pagetable_t</span> pagetable, uint64 va, uint64 size, uint64 pa, <span class="type">int</span> pte_flags)</span> {</span><br><span class="line">    <span class="keyword">for</span> (uint64 a = va; a &lt; va + size; a += PGSIZE) {</span><br><span class="line">        walk(pagetable, a, <span class="number">1</span>)[PTX(a)] = (pa + (a - va)) | (pte_flags | PTE_V);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></tbody></table></figure><ul><li><strong>作用</strong>：将虚拟地址范围映射到物理地址，并设置页表项的权限标志。</li></ul></li><li><p><strong><code>kvminit</code> 函数</strong>：</p><ul><li><strong>作用</strong>：初始化内核页表，映射内核代码、数据和堆栈。</li></ul></li><li><p><strong><code>kvmtrap</code> 函数</strong>：</p><ul><li><strong>作用</strong>：处理内核中的故障和中断，包括页面错误。</li></ul></li><li><p><strong><code>uservec</code> 和 <code>userret</code> 函数</strong>：</p><ul><li><strong>作用</strong>：<ul><li><code>uservec</code> 是用户态异常的汇编入口点，负责保存用户寄存器并切换到内核模式。</li><li><code>userret</code> 是返回用户态的汇编代码，负责恢复用户寄存器并切换回用户模式。</li></ul></li></ul></li></ol><hr><h3 id="学习建议"><a href="#学习建议" class="headerlink" title="学习建议"></a><strong>学习建议</strong></h3><ol><li><strong>阅读相关文档</strong>：<ul><li>仔细阅读 RISC-V 的分页硬件文档，了解页表结构和控制寄存器的使用。</li><li>查阅 xv6 的官方文档和注释，理解页面错误处理和内存管理的实现细节。</li></ul></li><li><strong>学习和理解源码</strong>：<ul><li>重点阅读和理解 <code>vm.c</code> 文件中的代码，特别是 <code>walk</code>、<code>mappages</code> 和 <code>kvmalloc</code> 等函数。</li><li>使用调试工具如 gdb 或 QEMU 的调试功能，逐步执行代码，观察页表和内存的变化。</li></ul></li><li><strong>动手实践</strong>：<ul><li>尝试修改 xv6 的内存管理代码，实现新的功能或优化现有实现。例如：<ul><li>实现更高效的内存分配算法。</li><li>添加对大页表的支持，减少页表的层次。</li><li>优化页面错误处理程序，提高性能。</li></ul></li></ul></li><li><strong>重复巩固知识点</strong>：<ul><li>对于复杂的概念，如分页模型和页表结构，需要多次复习和练习，确保能够熟练掌握。</li><li>通过编写小的测试程序或实验，验证对内存管理的理解。</li></ul></li></ol><h3 id="3-0-为什么需要页表"><a href="#3-0-为什么需要页表" class="headerlink" title="3.0 为什么需要页表"></a>3.0 为什么需要页表</h3><p>正如chapter2所说，每个进程都有自己独立的内存和地址空间。</p><p>这也就要求我们实现一定的内存<strong>强隔离</strong>，让进程之间不会相互影响。</p><p>但是，对于每个进程，我们不能都赋予一个定值的内存大小，这样<strong>吃大锅饭</strong>的行为会造成小进程的资源浪费，和大进程的资源不足。</p><p>页表是一种数据结构，就好像<strong>电话本</strong>一样，记录了其上所有表的地址。</p><p>页表使操作系统能够以 4096 ( 2 12 ) 字节对齐块的粒度控制虚拟到物理地址转换。</p><p>它们允许 xv6 隔离不同进程的地址空间并将它们复用到单个物理内存上。页表是一种流行的设计，因为它们提供了一定程度的间接性，允许操作系统执行许多技巧。</p><h3 id="3-1-Paging-hardware"><a href="#3-1-Paging-hardware" class="headerlink" title="3.1 Paging hardware"></a>3.1 Paging hardware</h3><p>CPU中执行指令使用的是虚拟地址。</p><p>RAM中执行内存操作使用的是物理地址</p><p>RISC-V 页表硬件通过将每个虚拟地址映射到物理地址来连接这两种地址。</p><h4 id="虚拟地址到物理地址的实现"><a href="#虚拟地址到物理地址的实现" class="headerlink" title="虚拟地址到物理地址的实现"></a>虚拟地址到物理地址的实现</h4><p>RISCV是64位机器，地址也就是64位。</p><p>在sv39配置中，我们只使用低39位，高25位不使用。</p><p>RISC-V 页表逻辑上是 2^27 (134,217,728) 的数组 <strong>page table entries(PTEs) 。</strong></p><p>每个 PTE 包含一个 44 位物理页号 (PPN) 和一些标志。</p><p><strong>分页硬件通过使用 39 位中的高 27 位索引到页表中找到 PTE 来翻译虚拟地址，并制作一个 56 位物理地址，其高 44位来自 PTE 中的 PPN，其低 12 位被复制来自原始虚拟地址Offset。</strong></p><p><code>PTE = PPN + flags</code> </p><p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250217135511476.png" alt="PTE的标志位"></p><p>flags这些标志位告诉分页硬件如何允许使用关联的虚拟地址。</p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250217133730129.png" alt="RISC-V 虚拟和物理地址，具有简化的逻辑页表" style="zoom:80%;"><h4 id="分级页表页"><a href="#分级页表页" class="headerlink" title="分级页表页"></a>分级页表页</h4><blockquote><p>页表作为三层树存储在物理内存中。树的根是一个 4096 字节的页表页，包含 512 个 PTE，其中包含树的下一级页表页的物理地址。每个页面都包含树中最终级别的 512 个 PTE。分页硬件使用这27 位中的前 9 位来选择根页表页中的 PTE，中间 9 位来选择树的下一级页表页中的 PTE，最后 9 位来选择期末 PTE。</p></blockquote><p>如果转换地址所需的三个 PTE 中的任何一个不存在，则分页硬件会引发 <em>page-fault</em></p><p><em>exception</em> ，将其留给内核来处理异常（请参阅第 4 章）。</p><p>对于分级页表页，我们可以与三级缓存一起理解，都是减少复杂性的结果。</p><blockquote><p>但三级结构的潜在缺点是 CPU 必须从内存加载三个 PTE 来执行加载/存储指令中的虚拟地址到物理地址的转换。为了避免从物理内存加载 PTE 的成本，RISC-V CPU 将页表条目缓存在 <em>Translation**Look-aside Buffer (TLB)</em> 。</p></blockquote><h4 id="对于不同进程，如何实现页表切换"><a href="#对于不同进程，如何实现页表切换" class="headerlink" title="对于不同进程，如何实现页表切换"></a>对于不同进程，如何实现页表切换</h4><p>为了告诉 CPU 使用页表，内核必须将根页表页的物理地址写入 satp 寄存器。CPU 将使用其自己的 satp 指向的页表来转换后续指令生成的所有地址。<strong>每个 CPU 都有自己的satp</strong> ，以便不同的 CPU 可以运行不同的进程，每个进程都有一个由自己的页表描述的私有地址空间。</p><p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250217135445031.png" alt="虚拟地址到物理地址的硬件机制"></p><h3 id="3-2-内核地址空间"><a href="#3-2-内核地址空间" class="headerlink" title="3.2 内核地址空间"></a><strong>3.2</strong> <strong>内核地址空间</strong></h3><p>在chapter2中，我们提到，进程拥有独立的地址空间。</p><p>分别是在用户空间和系统空间之中，所以我们也需要使用cpu来为进程维护页表状态。</p><blockquote><p>内核使用“直接映射”获取 RAM 和内存映射设备寄存器；即将资源映射到等于物理地址的虚拟地址。例如，内核本身在虚拟地址空间和物理内存中都位于 KERNBASE=0x80000000。</p></blockquote><p>内核本身也需要运行在虚拟地址空间中，但它需要能够直接访问物理内存。</p><p>通过直接映射，内核可以将自身加载到虚拟地址空间的某个固定位置（如 <code>KERNBASE=0x80000000</code>），同时这个虚拟地址空间直接对应物理内存的某个区域。</p><p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250217140436745.png" alt="如何将内核虚拟地址映射到物理地址"></p><p>有几个未直接映射的内核虚拟地址：</p><ul><li><p><strong>trampoline 页</strong></p><ul><li>它映射在虚拟地址空间的顶部；</li><li>用户页表具有相同的映射。</li><li>物理页（保存 trampoline 代码）在内核的虚拟地址空间中映射两次：一次在虚拟地址空间的顶部，一次是直接映射。</li></ul><p>==在<strong>用户空间和内核之间共享一个只读区域</strong>的数据来加速某些系统调用。这消除了在执行这些系统调用时需要跨越内核边界的需要。==</p></li><li><p><strong>内核堆栈页</strong></p><ul><li>每个进程都有自己的内核堆栈，该堆栈被映射到高位，以便在其下方xv6 可以留下未映射的 <em>guard page</em> 。</li><li>保护页面的 PTE 无效（即 PTE_V 未设置），因此如果内核溢出内核堆栈，很可能会导致异常并且内核会出现 Panic。如果没有保护页，溢出的堆栈将覆盖其他内核内存，从而导致不正确的操作。Panic 崩溃是更好的选择。</li></ul></li></ul><h3 id="3-3-代码：创建地址空间"><a href="#3-3-代码：创建地址空间" class="headerlink" title="3.3 代码：创建地址空间"></a><strong>3.3</strong> <strong>代码：创建地址空间</strong></h3><h4 id="1-xv6-虚拟内存管理的核心代码"><a href="#1-xv6-虚拟内存管理的核心代码" class="headerlink" title="1. xv6 虚拟内存管理的核心代码"></a>1. <strong>xv6 虚拟内存管理的核心代码</strong></h4><p>xv6 中与虚拟内存相关的代码主要集中在 <code>kernel/vm.c</code> 文件中。核心数据结构是 <code>pagetable_t</code>，它是一个指向 RISC-V <strong>根页表页</strong>的<strong>指针</strong>，可以表示内核页表或某个进程的用户页表。</p><h4 id="2-核心功能"><a href="#2-核心功能" class="headerlink" title="2. 核心功能"></a>2. <strong>核心功能</strong></h4><ul><li><strong><code>walk</code> 函数</strong>：用于查找虚拟地址对应的页表项（PTE）。它模拟 RISC-V 的分页硬件，逐级遍历页表，直到找到目标虚拟地址的 PTE。如果 PTE 无效且设置了分配标志（<code>alloc</code>），它会分配新的页表页并将其地址写入 PTE。</li><li><strong><code>mappages</code> 函数</strong>：用于安装新的虚拟地址到物理地址的映射。它会逐页处理虚拟地址范围，并为每个虚拟地址调用 <code>walk</code> 来查找或创建 PTE，然后设置 PTE 的权限和物理页号。</li></ul><h4 id="3-内核页表的初始化"><a href="#3-内核页表的初始化" class="headerlink" title="3. 内核页表的初始化"></a>3. <strong>内核页表的初始化</strong></h4><p>在 xv6 启动的早期阶段，<code>main</code> 函数会调用 <code>kvminit</code> 来<strong>初始化内核的页表</strong>：</p><ul><li><strong><code>kvmmake</code></strong>：负责创建内核页表。它首先分配物理内存页来保存根页表页，然后调用 <code>kvmmap</code> 安装内核所需的地址映射，包括内核代码、数据、物理内存（高达 <code>PHYSTOP</code>）以及设备内存范围。</li><li><strong><code>proc_mapstacks</code></strong>：为每个进程分配内核堆栈，并调用 <code>kvmmap</code> 将堆栈映射到虚拟地址空间，同时为无效的堆栈保护页留出空间。</li></ul><h4 id="4-页表映射的实现"><a href="#4-页表映射的实现" class="headerlink" title="4. 页表映射的实现"></a>4. <strong>页表映射的实现</strong></h4><ul><li><strong><code>kvmmap</code></strong>：调用 <code>mappages</code> 来安装虚拟地址到物理地址的映射。它逐页处理虚拟地址范围，并为每个虚拟地址调用 <code>walk</code> 来查找或创建 PTE，然后设置 PTE 的权限和物理页号。</li><li><strong><code>walk</code></strong>：逐级遍历页表，使用虚拟地址的高位来索引页表项。如果 PTE 无效且设置了分配标志，它会分配新的页表页，并将物理地址写入 PTE。</li></ul><h4 id="5-直接映射的依赖"><a href="#5-直接映射的依赖" class="headerlink" title="5. 直接映射的依赖"></a>5. <strong>直接映射的依赖</strong></h4><p>xv6 的内核虚拟地址空间通过直接映射的方式访问物理内存。例如：</p><ul><li>当 <code>walk</code> 函数遍历页表时，它会从 PTE 中提取下一级页表的物理地址，并直接将其作为虚拟地址使用，因为内核的虚拟地址空间与物理内存是直接映射的。</li></ul><h4 id="6-内核页表的安装"><a href="#6-内核页表的安装" class="headerlink" title="6. 内核页表的安装"></a>6. <strong>内核页表的安装</strong></h4><ul><li><strong><code>kvminithart</code></strong>：将内核页表的根页表页物理地址写入 <code>satp</code> 寄存器，使 CPU 开始使用内核页表进行地址转换。由于内核使用恒等映射，虚拟地址可以直接映射到物理地址。</li></ul><h4 id="7-TLB-管理"><a href="#7-TLB-管理" class="headerlink" title="7. TLB 管理"></a>7. <strong>TLB 管理</strong></h4><p>每个 RISC-V CPU 都有一个翻译后备缓冲区（TLB），用于缓存页表条目。当 xv6 修改页表时，必须通知 CPU 使相应的 TLB 条目失效，以防止旧的映射被错误使用：</p><ul><li><strong><code>sfence.vma</code> 指令</strong>：用于刷新当前 CPU 的 TLB。xv6 在以下场景中使用该指令：<ol><li>在 <code>kvminithart</code> 中，安装内核页表后刷新 TLB。</li><li>在切换到用户页表的 <code>trampoline</code> 代码中，返回用户空间前刷新 TLB。</li><li>在修改页表前，刷新 TLB 以确保所有未完成的加载和存储操作完成。</li></ol></li></ul><h4 id="8-地址空间标识符（ASID）"><a href="#8-地址空间标识符（ASID）" class="headerlink" title="8. 地址空间标识符（ASID）"></a>8. <strong>地址空间标识符（ASID）</strong></h4><p>RISC-V 支持地址空间标识符（ASID），用于区分不同进程的地址空间，从而避免刷新整个 TLB。然而，xv6 并没有使用这一功能。</p><hr><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>xv6 的虚拟内存管理通过 <code>kernel/vm.c</code> 文件中的核心函数（如 <code>walk</code> 和 <code>mappages</code>）实现页表的初始化和映射。内核页表在启动时通过 <code>kvminit</code> 和 <code>kvmmake</code> 初始化，并通过直接映射的方式访问物理内存。TLB 的刷新通过 <code>sfence.vma</code> 指令实现，以确保页表修改后地址转换的正确性。xv6 的设计简洁高效，尽管没有使用 ASID 功能，但仍然能够有效地管理虚拟内存和地址空间。</p><h3 id="3-4-物理内存分配"><a href="#3-4-物理内存分配" class="headerlink" title="3.4 物理内存分配"></a><strong>3.4</strong> <strong>物理内存分配</strong></h3><blockquote><p><strong>内核</strong>必须在运行时为页表、用户内存、内核堆栈和管道缓冲区分配和释放物理内存。xv6 使用内核末尾和 PHYSTOP 用于运行时分配。它一次分配和释放整个 4096 字节页面。它通过将链接列表穿过页面本身来跟踪哪些页面是空闲的。分配包括从链表中删除页面；释放包括将释放的页面添加到列表中。（freelist也就是加入一个空闲页面的链表之中)</p></blockquote><h3 id="3-5-代码：物理内存分配器"><a href="#3-5-代码：物理内存分配器" class="headerlink" title="3.5 代码：物理内存分配器"></a><strong>3.5</strong> <strong>代码：物理内存分配器</strong></h3><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">run</span> {</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">run</span> *<span class="title">next</span>;</span></span><br><span class="line">};</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> {</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">spinlock</span> <span class="title">lock</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">run</span> *<span class="title">freelist</span>;</span></span><br><span class="line">} kmem;</span><br></pre></td></tr></tbody></table></figure><p>分配器的数据结构是可用于分配的物理内存页的空闲列表。</p><p>每个空闲页面的列表元素是 struct run</p><h4 id="内存的分配过程"><a href="#内存的分配过程" class="headerlink" title="内存的分配过程"></a>内存的分配过程</h4><ol><li>main函数调用<code>kinit</code> 来初始化分配器（<code>kernel/kalloc.c:27</code>）。</li><li><code>kinit</code> 将空闲列表初始化为内核结束到 <code>PHYSTOP</code> 之间的所有页面。</li><li><code>kinit</code> 调用 <code>freerange</code>，通过逐页调用 <code>kfree</code> 将内存添加到空闲列表中。<ol><li>由于页表项（PTE）只能引用 4096 字节边界对齐的物理地址（即 4096 的倍数），<code>freerange</code> 使用 <code>PGROUNDUP</code> 来确保只释放对齐的物理地址。</li></ol></li></ol><blockquote><h3 id="对齐的物理地址及其重要性"><a href="#对齐的物理地址及其重要性" class="headerlink" title="对齐的物理地址及其重要性"></a>对齐的物理地址及其重要性</h3><p><strong>对齐的物理地址</strong>是指物理地址的值是某个特定值（如4096字节）的整数倍。在计算机系统中，特别是涉及到内存管理和页表操作时，地址对齐是一个重要的概念。例如，在RISC-V架构中，页表项（PTE）只能引用按4096字节边界对齐的物理地址。</p><h3 id="为什么只释放对齐的物理地址"><a href="#为什么只释放对齐的物理地址" class="headerlink" title="为什么只释放对齐的物理地址"></a>为什么只释放对齐的物理地址</h3><ol><li><strong>页表管理的需要</strong><br>在现代操作系统中，物理内存通常以<strong>页（Page）<strong>为单位进行管理，每一页的大小通常是4096字节。页表项（PTE）用于将虚拟地址映射到物理地址，而</strong>PTE只能指向按4096字节对齐的物理地址</strong>。因此，操作系统在分配和释放内存时，必须确保物理地址是对齐的，以符合页表项的要求。</li><li><strong>简化内存管理</strong><br>使用对齐的物理地址可以简化内存分配器的设计。例如，xv6的物理内存分配器通过<code>freerange</code>函数将内存添加到空闲列表中，而<code>freerange</code>使用<code>PGROUNDUP</code>宏来确保只处理对齐的物理地址。这样<strong>可以避免处理非对齐地址带来的复杂性</strong>，同时确保内存分配和释放的高效性。</li><li><strong>硬件和性能优化</strong><br>硬件通常对地址对齐有特定要求，对齐的地址可以提高内存访问的效率。非对齐地址可能导致额外的硬件开销，甚至可能引发错误。因此，操作系统在设计内存管理机制时，会优先使用对齐的地址。</li><li><strong>避免错误和未定义行为</strong><br>如果释放的物理地址不是对齐的，可能会导致页表项指向无效的物理地址，从而引发硬件错误或未定义行为。为了避免这些问题，操作系统在设计内存管理机制时，会确保只释放对齐的物理地址。</li></ol><h3 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h3><p>对齐的物理地址是内存管理中的一个重要概念，它确保了页表项能够正确地引用物理内存，并简化了内存分配器的设计。在xv6中，<code>freerange</code>函数通过<code>PGROUNDUP</code>宏确保只释放对齐的物理地址，从而满足页表项的要求并优化系统性能。</p></blockquote><h3 id="3-6-进程地址空间"><a href="#3-6-进程地址空间" class="headerlink" title="3.6 进程地址空间"></a><strong>3.6</strong> <strong>进程地址空间</strong></h3><p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250217162900087.png" alt="进程的用户地址空间及其初始堆栈。"></p><hr><h4 id="1-进程地址空间的隔离与切换"><a href="#1-进程地址空间的隔离与切换" class="headerlink" title="1. 进程地址空间的隔离与切换"></a><strong>1. 进程地址空间的隔离与切换</strong></h4><ul><li><strong>每个进程的独立页表</strong>：在 xv6 中，每个进程都有一个独立的页表。页表的作用是将虚拟地址映射到物理地址。</li><li><strong>进程切换与页表切换</strong>：当 xv6 在不同进程之间切换时，它不仅切换进程的上下文，还会切换页表。这意味着每个进程都有自己独立的地址空间，进程之间无法直接访问彼此的内存。</li></ul><h4 id="2-用户内存的布局与寻址范围"><a href="#2-用户内存的布局与寻址范围" class="headerlink" title="2. 用户内存的布局与寻址范围"></a><strong>2. 用户内存的布局与寻址范围</strong></h4><ul><li><strong>用户内存的起始地址</strong>：每个进程的用户内存从虚拟地址 <code>0</code> 开始。</li><li><strong>寻址范围</strong>：用户内存的上限由 <code>MAXVA</code> 定义（在 <code>kernel/riscv.h:360</code> 中）。理论上，xv6 的进程可以寻址高达 <strong>256 GB</strong> 的内存空间。</li><li><strong>实际使用</strong>：尽管寻址范围很大，但大多数进程并不会使用整个用户地址空间。未使用的页表项（PTE）的 <code>PTE_V</code> 标志会被清零，表示这些地址空间未被映射到物理内存。</li></ul><h4 id="3-用户内存的动态分配"><a href="#3-用户内存的动态分配" class="headerlink" title="3. 用户内存的动态分配"></a><strong>3. 用户内存的动态分配</strong></h4><ul><li><strong>分配物理页面</strong>：当进程请求更多用户内存时，xv6 使用以下步骤分配内存：<ol><li><strong>调用 <code>kalloc</code></strong>：通过内核的物理内存分配器 <code>kalloc</code> 分配物理页面。</li><li><strong>更新页表</strong>：为新分配的物理页面在进程的页表中添加页表项（PTE），并设置以下标志：<ul><li><code>PTE_W</code>（可写）</li><li><code>PTE_X</code>（可执行）</li><li><code>PTE_R</code>（可读）</li><li><code>PTE_U</code>（用户模式可访问）</li><li><code>PTE_V</code>（页表项有效）</li></ul></li></ol></li><li><strong>页表的作用</strong>：<ul><li><strong>隔离性</strong>：不同进程的页表将用户地址映射到不同的物理页面，确保每个进程的用户内存是私有的。</li><li><strong>虚拟地址连续性</strong>：进程看到的内存是虚拟地址从 <code>0</code> 开始的连续空间，但实际的物理内存可以是不连续的。</li><li><strong>共享页面</strong>：内核在用户地址空间的顶部映射了一个包含 trampoline 代码的页面。这个页面在所有进程的地址空间中都可见，但只占用一个物理页面。</li></ul></li></ul><h4 id="4-用户内存布局的详细结构"><a href="#4-用户内存布局的详细结构" class="headerlink" title="4. 用户内存布局的详细结构"></a><strong>4. 用户内存布局的详细结构</strong></h4><ul><li><strong>栈的布局</strong>：<ul><li><strong>栈的大小</strong>：用户栈是一个单独的页面（通常为 4KB）。</li><li><strong>栈的初始内容</strong>：由 <code>exec</code> 系统调用创建，栈的顶部包含以下内容：<ol><li><strong>命令行参数字符串</strong>：存储用户输入的命令行参数。</li><li><strong>指针数组</strong>：指向命令行参数字符串的指针数组。</li><li><strong>启动参数</strong>：包含 <code>argc</code> 和 <code>argv</code>，使得程序可以从 <code>main</code> 函数开始运行，就好像调用了 <code>main(argc, argv)</code>。</li></ol></li></ul></li><li><strong>栈溢出保护</strong>：<ul><li><strong>保护页</strong>：为了防止用户栈溢出，xv6 在栈的下方放置了一个不可访问的保护页。</li><li><strong>保护机制</strong>：通过清除保护页的 <code>PTE_U</code> 标志，使其对用户模式程序不可访问。</li><li><strong>异常处理</strong>：如果栈溢出，进程尝试访问保护页时，硬件会触发页面错误异常（page-fault exception）。</li><li><strong>现实系统的处理方式</strong>：在真实操作系统中，可能会在栈溢出时自动分配更多内存，而不是直接触发异常。</li></ul></li></ul><h4 id="5-页表的其他应用"><a href="#5-页表的其他应用" class="headerlink" title="5. 页表的其他应用"></a><strong>5. 页表的其他应用</strong></h4><ul><li><strong>内核代码映射</strong>：内核在用户地址空间的顶部映射了一个包含 trampoline 代码的页面。这个页面在所有进程的地址空间中都可见，但只占用一个物理页面。</li><li><strong>内存隔离与共享</strong>：通过页表，xv6 实现了进程之间的内存隔离，同时允许某些页面（如 trampoline 页面）在多个进程之间共享。</li></ul><hr><h4 id="总结-2"><a href="#总结-2" class="headerlink" title="总结"></a><strong>总结</strong></h4><p>xv6 通过为每个进程维护独立的页表，实现了进程之间的内存隔离和地址空间管理。用户内存从虚拟地址 <code>0</code> 开始，理论上可以寻址高达 256 GB 的内存空间，但实际上大多数进程只使用其中的一部分。页表项（PTE）的标志位用于控制页面的访问权限。用户栈是一个单独的页面，初始内容由 <code>exec</code> 创建，栈的下方有一个不可访问的保护页用于防止栈溢出。通过这些机制，xv6 在保证内存隔离的同时，也实现了内存的高效利用和保护。</p><h3 id="3-7-代码：sbrk"><a href="#3-7-代码：sbrk" class="headerlink" title="3.7 代码：sbrk"></a><strong>3.7</strong> 代码：sbrk</h3><p>sbrk 是一个进程用来<strong>收缩或扩展其内存</strong>的系统调用。该系统调用由 growproc 函数实现（kernel/proc.c:253）。</p><ul><li>growproc 根据参数 n 是正数还是负数，调用 uvmalloc 或 uvmdealloc。<ul><li>uvmalloc（kernel/vm.c:221）使用 kalloc 分配物理内存，并通过 mappages 将页表项（PTEs）添加到用户页表中。</li><li>uvmdealloc 调用 uvmunmap（kernel/vm.c:166），后者使用 walk 查找 PTEs，并通过 kfree 释放它们指向的物理内存。</li></ul></li></ul><p>xv6 使用进程的页表不仅是为了告诉硬件如何映射用户虚拟地址，而且<strong>作为记录分配给该进程的物理内存页面的唯一记录</strong>。这就是为什么释放用户内存（在 uvmunmap 中）需要检查用户页表的原因。</p><h3 id="3-8-代码：exec"><a href="#3-8-代码：exec" class="headerlink" title="3.8 代码：exec"></a>3.8 代码：exec</h3><ul><li><strong>功能</strong>：exec 用于创建地址空间的用户部分，从文件系统中的文件初始化用户空间。</li><li><strong>核心步骤</strong>：<ol><li><strong>检查文件格式</strong>：通过 ELF 头的“魔数”验证文件是否为有效的 ELF 二进制文件。</li><li><strong>分配页表</strong>：通过 <code>proc_pagetable</code> 创建一个没有用户映射的新页表。</li><li><strong>加载程序段</strong>：使用 <code>uvmalloc</code> 分配内存，并通过 <code>loadseg</code> 将 ELF 段加载到内存中。</li><li><strong>初始化用户栈</strong>：分配一个栈页，将参数字符串复制到栈顶，并在栈末尾放置空指针。</li><li><strong>保护机制</strong>：在栈页下方放置一个不可访问的页面，防止程序栈溢出。</li></ol></li><li><strong>安全性问题</strong>：<ul><li>ELF 文件中的地址可能指向内核空间，导致内核崩溃或安全漏洞。</li><li>xv6 通过检查溢出等手段避免这些问题，但在复杂场景下仍可能存在漏洞。</li></ul></li></ul><h3 id="3-9-现实世界中的考虑"><a href="#3-9-现实世界中的考虑" class="headerlink" title="3.9 现实世界中的考虑"></a>3.9 现实世界中的考虑</h3><ul><li><strong>分页硬件的使用</strong>：xv6 使用分页硬件进行内存保护和映射，但相对简单，缺乏高级特性（如页错误处理）。</li><li><strong>物理地址映射问题</strong>：xv6 假设地址 0x8000000 处有物理 RAM，这在真实硬件上可能导致问题，因为真实硬件的物理地址布局是不可预测的。</li><li><strong>内存管理优化</strong>：<ul><li>RISC-V 支持“超级页”，适用于大内存机器，可减少页表操作开销。</li><li>xv6 缺乏动态内存分配器（如 malloc），限制了复杂数据结构的使用。</li><li>现实中的内核需要支持多种大小的内存分配，以提高效率和灵活性。</li></ul></li></ul>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 操作系统 </tag>
            
            <tag> MIT6.s081 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Lab2 syscall</title>
      <link href="/2025/02/16/lab2-syscall/"/>
      <url>/2025/02/16/lab2-syscall/</url>
      
        <content type="html"><![CDATA[<h2 id="System-call-tracing-moderate"><a href="#System-call-tracing-moderate" class="headerlink" title="System call tracing (moderate)"></a>System call tracing (<a href="https://pdos.csail.mit.edu/6.S081/2021/labs/guidance.html">moderate</a>)</h2><h4 id="题目"><a href="#题目" class="headerlink" title="题目"></a>题目</h4><p>系统调用跟踪（中等难度）</p><p>在这个任务中，你需要添加一个系统调用跟踪功能，这可能有助于你在后续实验中进行调试。你将创建一个新的跟踪系统调用，用于控制跟踪功能。</p><p><strong>该系统调用应接受一个参数，即一个整数“mask”，其位用于指定要跟踪的系统调用。</strong></p><p>例如，要跟踪 <code>fork</code> 系统调用，程序可以调用 <code>trace(1 &lt;&lt; SYS_fork)</code>，其中 <code>SYS_fork</code> 是内核头文件 <code>kernel/syscall.h</code> 中的一个系统调用编号。</p><p>你需要修改 xv6 内核，以便在每个系统调用即将返回时打印一行信息，前提是该系统调用的编号在 mask 中被设置。</p><p><strong>该行应包含进程 ID、系统调用的名称以及返回值；</strong></p><p>你无需打印系统调用的参数。跟踪系统调用应为调用它的进程及其后续 fork 的子进程启用跟踪功能，但不应影响其他进程。</p><p>我们提供了一个用户级程序 <code>trace</code>，它可以以启用跟踪的方式运行另一个程序（请参见 <code>user/trace.c</code>）。完成后，你应该看到类似以下的输出：</p><p>复制</p><figure class="highlight plaintext"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">$ trace 32 grep hello README  </span><br><span class="line">3: syscall read -&gt; 1023  </span><br><span class="line">3: syscall read -&gt; 966  </span><br><span class="line">3: syscall read -&gt; 70  </span><br><span class="line">3: syscall read -&gt; 0  </span><br><span class="line">$  </span><br><span class="line">$ trace 2147483647 grep hello README  </span><br><span class="line">4: syscall trace -&gt; 0  </span><br><span class="line">4: syscall exec -&gt; 3  </span><br><span class="line">4: syscall open -&gt; 3  </span><br><span class="line">4: syscall read -&gt; 1023  </span><br><span class="line">4: syscall read -&gt; 966  </span><br><span class="line">4: syscall read -&gt; 70  </span><br><span class="line">4: syscall read -&gt; 0  </span><br><span class="line">4: syscall close -&gt; 0  </span><br><span class="line">$  </span><br><span class="line">$ grep hello README  </span><br><span class="line">$  </span><br><span class="line">$ trace 2 usertests forkforkfork  </span><br><span class="line">usertests starting  </span><br><span class="line">test forkforkfork: 407: syscall fork -&gt; 408  </span><br><span class="line">408: syscall fork -&gt; 409  </span><br><span class="line">409: syscall fork -&gt; 410  </span><br><span class="line">410: syscall fork -&gt; 411  </span><br><span class="line">409: syscall fork -&gt; 412  </span><br><span class="line">410: syscall fork -&gt; 413  </span><br><span class="line">409: syscall fork -&gt; 414  </span><br><span class="line">411: syscall fork -&gt; 415  </span><br><span class="line">...  </span><br><span class="line">$    </span><br></pre></td></tr></tbody></table></figure><p>在上面的第一个例子中，<code>trace</code> 调用了 <code>grep</code>，仅跟踪 <code>read</code> 系统调用。数字 32 是 <code>1&lt;&lt;SYS_read</code>。在第二个例子中，<code>trace</code> 在跟踪所有系统调用的情况下运行 <code>grep</code>；数字 2147483647 的低 31 位都被设置。在第三个例子中，程序未被跟踪，因此没有跟踪输出。在第四个例子中，<code>usertests</code> 中的 <code>forkforkfork</code> 测试的所有后代进程的 <code>fork</code> 系统调用都被跟踪。如果你的程序表现如上所示（尽管进程 ID 可能不同），则你的解决方案是正确的。</p><p>一些提示：</p><ul><li>在 Makefile 中将 <code>$U/_trace</code> 添加到 <code>UPROGS</code> 中。</li><li>运行 <code>make qemu</code>，你会看到编译器无法编译 <code>user/trace.c</code>，因为用户空间的系统调用存根尚不存在：需要在 <code>user/user.h</code> 中添加系统调用的原型，在 <code>user/usys.pl</code> 中添加存根，并在 <code>kernel/syscall.h</code> 中添加一个系统调用编号。Makefile 调用了 Perl 脚本 <code>user/usys.pl</code>，它生成 <code>user/usys.S</code>，即实际的系统调用存根，这些存根使用 RISC-V 的 <code>ecall</code> 指令切换到内核。修复编译问题后，运行 <code>trace 32 grep hello README</code>；它会失败，因为你尚未在内核中实现该系统调用。</li><li>在 <code>kernel/sysproc.c</code> 中添加一个 <code>sys_trace()</code> 函数，用于通过在 <code>proc</code> 结构（请参见 <code>kernel/proc.h</code>）中的<strong>一个新变量中记住其参数来实现新的系统调用</strong>。从用户空间检索系统调用参数的函数在 <code>kernel/syscall.c</code> 中，你可以在 <code>kernel/sysproc.c</code> 中看到这些函数的使用示例。</li><li>修改 <code>fork()</code>（请参见 <code>kernel/proc.c</code>），以便将跟踪 mask 从父进程复制到子进程。</li><li>修改 <code>kernel/syscall.c</code> 中的 <code>syscall()</code> 函数以打印跟踪输出。你需要添加一个系统调用名称的数组以便索引。</li></ul><h4 id="解题思考"><a href="#解题思考" class="headerlink" title="解题思考"></a>解题思考</h4><ol><li><code>sys_trace()</code> 函数，用于通过在 <code>proc</code> 结构（请参见 <code>kernel/proc.h</code>）中的<strong>一个新变量中记住其参数来实现新的系统调用</strong>。</li><li>修改 <code>fork()</code>（请参见 <code>kernel/proc.c</code>），以便将跟踪 mask 从父进程复制到子进程。</li><li>修改 <code>kernel/syscall.c</code> 中的 <code>syscall()</code> 函数以打印跟踪输出。你需要添加一个系统调用名称的数组以便索引。</li></ol><p>我们通过trace来实现对于输入参数的处理，然后需要进入系统调用。</p><p>sys_trace就是系统调用函数。</p><p>fork实现将mask从父进程复制到子进程，也就是说proc中需要有一个来保存mask，同时mask是需要作为参数来获取的。</p><p>syscall来打印输出</p><h4 id="一些发现"><a href="#一些发现" class="headerlink" title="一些发现"></a>一些发现</h4><ul><li><p>整个user部分的函数，都在user.h中声明</p></li><li><p>实际的系统调用存根，这些存根使用 RISC-V 的 <code>ecall</code> 指令切换到内核。<strong>所以要创建用户空间的系统调用存根</strong></p></li><li><p>从用户空间检索系统调用参数的函数在 <code>kernel/syscall.c</code> 中<strong>从用户空间转到内核空间，需要通过特殊的函数来传递参数</strong></p></li></ul><h2 id="系统信息（中等难度）"><a href="#系统信息（中等难度）" class="headerlink" title="系统信息（中等难度）"></a>系统信息（中等难度）</h2><h3 id="题目-1"><a href="#题目-1" class="headerlink" title="题目"></a>题目</h3><p>在这个任务中，你需要添加一个系统调用 <code>sysinfo</code>，用于收集运行系统的相关信息。该系统<strong>调用接受一个参数</strong>：一个指向 <code>struct sysinfo</code> 的指针（请参见 <code>kernel/sysinfo.h</code>）。<strong>内核需要填充这个结构体的字段</strong>：<code>freemem</code> 字段应设置为可用内存的字节数，<code>nproc</code> 字段应设置为状态不是 <code>UNUSED</code> 的进程数量。我们提供了一个测试程序 <code>sysinfotest</code>；如果它打印出“<code>sysinfotest: OK</code>”，则表示你通过了这个任务。</p><p>一些提示：</p><ol><li><p>在 <code>Makefile</code> 中将 <code>$U/_sysinfotest</code> 添加到 <code>UPROGS</code> 中。</p></li><li><p>运行 <code>make qemu</code>；<code>user/sysinfotest.c</code> 将无法编译。按照上一个任务中的相同步骤添加系统调用 <code>sysinfo</code>。为了在 <code>user/user.h</code> 中声明 <code>sysinfo()</code> 的原型，你需要<strong>预先声明</strong> <code>struct sysinfo</code> 的存在：</p><p>c复制</p><figure class="highlight c"><table><tbody><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sysinfo</span>;</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sysinfo</span><span class="params">(<span class="keyword">struct</span> sysinfo *)</span>;</span><br></pre></td></tr></tbody></table></figure></li><li><p>修复编译问题后，运行 <code>sysinfotest</code>；它会失败，因为你尚未在内核中实现该系统调用。</p></li><li><p><code>sysinfo</code> 需要将一个 <code>struct sysinfo</code> 结构体拷贝回用户空间；可以参考 <code>sys_fstat()</code>（位于 <code>kernel/sysfile.c</code>）和 <code>filestat()</code>（位于 <code>kernel/file.c</code>），了解如何使用 <code>copyout()</code> 来实现这一点。</p></li><li><p>为了收集可用内存的数量，需要在 <code>kernel/kalloc.c</code> 中添加一个函数。</p></li><li><p>为了收集进程的数量，需要在 <code>kernel/proc.c</code> 中添加一个函数。</p></li></ol><p><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250217112148863.png" alt="sysinfo分析"></p>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 操作系统 </tag>
            
            <tag> MIT6.s081 </tag>
            
            <tag> lab </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Chapter 2 复用，隔离和交互——操作系统的三大要点</title>
      <link href="/2025/02/16/cao-zuo-xi-tong-lec2/"/>
      <url>/2025/02/16/cao-zuo-xi-tong-lec2/</url>
      
        <content type="html"><![CDATA[<p><strong>Chapter 2: Operating System Organization</strong> provides a deeper look at how an operating system, particularly xv6, is structured to manage processes, resources, and ensure isolation while enabling interaction between processes. Here’s a breakdown based on your request:</p><h3 id="重要要点："><a href="#重要要点：" class="headerlink" title="重要要点："></a>重要要点：</h3><ul><li><strong>进程管理与资源共享</strong>：操作系统需要管理多个并发进程，并确保每个进程有机会执行，即使 CPU 数量少于进程数。操作系统必须平衡 <strong>复用</strong>（共享资源）、<strong>隔离</strong>（防止进程互相干扰）、<strong>交互</strong>（允许进程间的通信）。</li><li><strong>内核结构与设计</strong>：本章介绍了 xv6 的内核组织，着重于单片内核（monolithic kernel）设计。虽然有些操作系统采用微内核设计（microkernel），xv6 选择了简单且高效的整体内核设计。</li></ul><h3 id="关键概念和名词："><a href="#关键概念和名词：" class="headerlink" title="关键概念和名词："></a>关键概念和名词：</h3><ul><li><strong>Monolithic Kernel</strong>：一种将操作系统所有功能集成在一个内核中的设计。xv6 使用这种设计，其中所有的系统服务都运行在管理模式下。</li><li><strong>Microkernel</strong>：与单片内核相对的设计，核心功能最小化，很多服务在用户空间运行。</li><li><strong>System Calls</strong>：系统调用接口，允许用户进程请求内核提供服务，如 <code>fork</code>、<code>exec</code> 等。</li><li><strong>Page Table</strong>：页表是用于管理进程虚拟内存的结构，它帮助将虚拟地址映射到物理内存地址。</li><li><strong>User Mode &amp; Kernel Mode</strong>：两种CPU执行模式，用户模式下的应用程序无法直接访问操作系统资源，而管理模式（内核模式）下的操作系统可以访问所有硬件资源。</li><li><strong>Fork &amp; Exec</strong>：<code>fork</code> 用于创建新进程，<code>exec</code> 用于加载新程序并替换当前进程的内存映像。</li></ul><h3 id="需要了解的背景知识："><a href="#需要了解的背景知识：" class="headerlink" title="需要了解的背景知识："></a>需要了解的背景知识：</h3><ul><li><strong>进程管理</strong>：理解如何创建和管理进程，以及如何通过系统调用实现进程间的调度与通信。</li><li><strong>内存管理</strong>：掌握虚拟内存和物理内存之间的关系，理解如何通过页表进行内存映射。</li><li><strong>RISC-V 架构</strong>：本章介绍了与 RISC-V 架构相关的一些概念，如不同的 CPU 执行模式（用户模式、管理员模式、机器模式），以及如何在不同模式下切换。</li></ul><h3 id="学习目标："><a href="#学习目标：" class="headerlink" title="学习目标："></a>学习目标：</h3><ul><li>了解操作系统如何组织资源和管理多个并发进程。</li><li>能够理解进程的隔离、共享和交互机制。</li><li>理解 xv6 如何实现进程的内存管理和调度，并能够跟踪系统调用的执行过程。</li><li>掌握系统调用的实现与内核模式的切换机制。</li></ul><h3 id="重要代码和函数："><a href="#重要代码和函数：" class="headerlink" title="重要代码和函数："></a>重要代码和函数：</h3><ul><li><strong><code>fork()</code></strong>：创建一个新进程并复制父进程的内存空间，返回新进程的 PID 给父进程，返回 0 给子进程。</li><li><strong><code>exec()</code></strong>：加载并执行指定的可执行文件，替换当前进程的内存映像。</li><li><strong><code>page_table</code></strong>：维护每个进程的虚拟内存空间，确保进程内存的隔离。</li><li><strong><code>ecall</code> &amp; <code>mret</code></strong>：<code>ecall</code> 指令用于从用户模式切换到内核模式，<code>mret</code> 用于从内核模式返回用户模式。</li></ul><h3 id="学习建议："><a href="#学习建议：" class="headerlink" title="学习建议："></a>学习建议：</h3><ul><li><strong>实验实践</strong>：在理解这些概念后，进行实验并通过代码实现系统调用、进程管理等功能。</li><li><strong>文档阅读</strong>：细读 xv6 的源代码，特别是涉及进程创建、系统调用处理和内存管理的部分。</li></ul><h3 id="2-1抽象物理资源"><a href="#2-1抽象物理资源" class="headerlink" title="2.1抽象物理资源"></a>2.1抽象物理资源</h3><p>应用程序通过与系统调用形成的库链接，然后调用硬件资源。<br>操作系统所承担的是中间层的责任。</p><p><code>所谓分层的思想，本质上是提供服务和隔离. 下层通过向上层暴露接口来为上层提供服务。 上下两层负责不同的任务，也将任务拆分成了两个部分，两层之中有一层出问题，另一层受到的影响也是有限的</code></p><p>因为需要通过库来使用硬件资源，这就要求一个进程不能长时间地占用硬件服务。也就是需要实现进程间的<strong>time-sharing</strong><br>但是在每个进程看来自己的优先级是最高的，这也就需要一个<strong>大家长</strong>来实现资源的分配，也就需要<strong>进程间的隔离</strong>。</p><p>而通过将硬件资源抽象为服务，可以实现强隔离，禁止应用程序直接访问硬件资源</p><p><code>Unix 应用程序仅通过文件系统的 open , read , write 和 close 系统调用， 而不是直接读写磁盘。这为应用程序提供了路径名的便利，并且允许操作系统（作为接口 的实现者）管理磁盘。即使隔离不是一个问题，有意交互（或只是希望不妨碍彼此）的程 序也可能会发现文件系统是比直接使用磁盘更方便的抽象。</code></p><h4 id="如何实现time-sharing"><a href="#如何实现time-sharing" class="headerlink" title="如何实现time-sharing"></a>如何实现time-sharing</h4><p>通过给每个进程赋予不同的时间片。<br>使得看起来好像多个进程在同时运行，但本质上是串行运行</p><p><code>Unix 在进程之间透明地切换硬件 CPU，根据需要保存和恢复寄存器状态</code></p><h3 id="2-2-用户模式、管理员模式和系统调用"><a href="#2-2-用户模式、管理员模式和系统调用" class="headerlink" title="2.2 用户模式、管理员模式和系统调用"></a>2.2 用户模式、管理员模式和系统调用</h3><blockquote><p>强隔离需要应用程序和操作系统之间有硬边界。<br>CPU 为强隔离提供硬件支持。例如，RISC-V 具有三种 CPU 执行指令的模式：machine<br>mode , supervisor mode 和 user mode 。在机器模式下执行的指令具有完全特权；CPU 以机<br>器模式启动。机器模式主要用于配置计算机。Xv6 在机器模式下执行几行，然后更改为管<br>理模式。</p></blockquote><p>使用多种模式的本质是权力的拆分，也就是隔离。</p><p>想要执行特权指令，调用内核函数的应用程序。<br>都需要切换到管理员模式，从userspace转移到kernelspace</p><blockquote><p>应用程序不能直接调用内核函数。CPU 提供了一种特殊的指令，可以将 CPU 从用户模式切换到<br>管理模式，并在内核指定的入口点进入内核。（RISC-V 提供 ecall 指令用于此目的。）一旦 CPU 切换到管理模式，内核就可以验证系统调用的参数（例如，检查传递给系统调用的<br>地址是否是应用程序内存的一部分），决定应用程序是否允许执行请求的操作（例如，检<br>查应用程序是否允许写入指定的文件），然后拒绝或执行它。内核控制转换到管理模式的<br>入口点非常重要；例如，如果应用程序可以决定内核入口点，则恶意应用程序可以在跳过<br>参数验证的点进入内核。</p></blockquote><p><img src="https://raw.githubusercontent.com/erinmi4/images/d6ade7053a0d42550695570eee3ea09572e75003/hexo/%E5%B8%A6%E6%9C%89%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%9A%84%E5%BE%AE%E5%86%85%E6%A0%B8.png" alt="带有文件系统服务器的微内核"></p><h3 id="2-3-内核组织"><a href="#2-3-内核组织" class="headerlink" title="2.3 内核组织"></a>2.3 内核组织</h3><ul><li>一个关键的设计问题：<strong>操作系统的那一部分放到内核之中</strong></li></ul><h4 id="monolithic-kernel整体内核"><a href="#monolithic-kernel整体内核" class="headerlink" title="monolithic kernel整体内核"></a>monolithic kernel整体内核</h4><ul><li>将整个整个操作系统驻留在内核中，以便所有系统调用的实现都在管理程序模式下运行。</li></ul><p>优点：有一个<strong>大家长</strong>负责包办所有的事情，不必要区分什么是要做的，什么是不要做的。</p><p>缺点：内部接口复杂，把复杂性全部集中到了内核上，遇到问题，更容易树倒猢狲散。</p><h4 id="microkernel微内核"><a href="#microkernel微内核" class="headerlink" title="microkernel微内核"></a>microkernel微内核</h4><blockquote><p>为了降低内核出错的风险，操作系统设计者可以最大限度地减少在管理模式下运行的操作系统代码量，并<strong>在用户模式下执行操作系统的大部分</strong>代码。这个内核组织称为<br>microkernel 。</p></blockquote><p>两种内核并不存在哪一个种<strong>更好</strong>的结论</p><p>整体内核速度更快，微内核更加稳定。</p><h3 id="2-5-流程概览"><a href="#2-5-流程概览" class="headerlink" title="2.5 流程概览"></a>2.5 流程概览</h3><h4 id="进程抽象"><a href="#进程抽象" class="headerlink" title="进程抽象"></a>进程抽象</h4><p>在 xv6 和其他类 Unix 操作系统中，<strong>进程</strong>是最基本的隔离单位。进程的抽象防止一个进程能够破坏或监视其他进程的内存、CPU 和文件描述符等资源。此外，进程抽象还能够防止进程影响操作系统内核本身，因此，内核需要谨慎实现进程抽象，因为恶意应用程序可能会试图规避隔离措施。</p><p>进程抽象通过以下几种机制来实现：</p><ul><li><strong>用户/管理程序模式标志</strong>：CPU 会通过标志位来区分代码是处于用户模式还是管理模式。</li><li><strong>地址空间</strong>：每个进程都有自己的虚拟地址空间，防止进程间相互访问。</li><li><strong>时间分片</strong>：操作系统对进程进行时间分配，保证每个进程都有机会执行。</li></ul><h5 id="为什么需要进程"><a href="#为什么需要进程" class="headerlink" title="为什么需要进程"></a>为什么需要进程</h5><p>进程抽象通过为每个进程提供独立的“幻觉”，使得每个进程都认为自己是唯一在运行的程序。实际上，操作系统通过时间分片技术，确保多个进程能并发执行，从而有效利用 CPU。</p><h4 id="进程的实现"><a href="#进程的实现" class="headerlink" title="进程的实现"></a>进程的实现</h4><p>在 xv6 中，内核为每个进程维护许多状态信息，保存在 <code>struct proc</code> 中。这些状态信息包括进程的页表、内核堆栈和运行状态。</p><h5 id="如何实现看似独立的内存"><a href="#如何实现看似独立的内存" class="headerlink" title="如何实现看似独立的内存"></a>如何实现看似独立的内存</h5><p>xv6 为每个进程维护一个单独的 <strong>页表</strong>，以定义该进程的地址空间。进程的虚拟内存从虚拟地址 0 开始，包括以下部分：</p><ul><li><strong>指令区</strong>：存储程序代码。</li><li><strong>全局变量</strong>：存储全局变量的内存区域。</li><li><strong>堆栈区</strong>：存储局部变量和函数调用的堆栈。</li><li><strong>堆区</strong>：用于动态分配内存（如通过 <code>malloc</code>）。</li></ul><p>为了支持从用户空间切换到内核空间，xv6 使用两个特殊的页面：</p><ul><li><strong>trampoline 页</strong>：用于代码转换和进程从用户模式返回到内核模式。</li><li><strong>trapframe 页</strong>：用于保存和恢复用户进程的状态，以便进行上下文切换。</li></ul><img src="https://raw.githubusercontent.com/erinmi4/images/master/hexo/image-20250217113315930.png" alt="进程虚拟地址空间的布局" style="zoom: 33%;"><h5 id="如何实现独有的-CPU"><a href="#如何实现独有的-CPU" class="headerlink" title="如何实现独有的 CPU"></a>如何实现独有的 CPU</h5><p>每个进程都有一个执行线程，并且进程在运行时会切换线程的上下文。线程的状态（如局部变量、函数调用的返回地址）会保存在 <strong>堆栈</strong> 中。进程切换时，内核保存当前进程的状态，并加载另一个进程的状态。</p><p>每个进程都有两个堆栈：<strong>用户堆栈</strong> 和 <strong>内核堆栈</strong>。当进程执行用户代码时，仅使用用户堆栈；当进入内核模式（如进行系统调用时），则使用内核堆栈。</p><p>通过使用 <code>ecall</code> 指令，进程可以从用户模式切换到内核模式，执行内核中的系统调用。</p><h5 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h5><p>进程抽象包含两种重要设计思想：</p><ol><li><strong>地址空间</strong>：为每个进程提供了独立的内存空间，使得它们的内存不会互相干扰。</li><li><strong>线程</strong>：为每个进程提供了独立的执行线程，使得每个进程能够独立执行。</li></ol><p>通过这两种机制，操作系统能够有效地管理多个并发进程，并确保它们之间的隔离和独立性。</p><hr><h3 id="2-6-启动-xv6：第一个进程和系统调用"><a href="#2-6-启动-xv6：第一个进程和系统调用" class="headerlink" title="2.6 启动 xv6：第一个进程和系统调用"></a>2.6 启动 xv6：第一个进程和系统调用</h3><h4 id="1-计算机启动和引导加载程序"><a href="#1-计算机启动和引导加载程序" class="headerlink" title="1. 计算机启动和引导加载程序"></a>1. 计算机启动和引导加载程序</h4><p>当 RISC-V 计算机启动时，首先进行自我初始化。启动过程从只读存储器（ROM）中的 <strong>引导加载程序</strong> 开始，作用是将操作系统内核（xv6）加载到内存中。内核被加载到物理地址 <strong>0x80000000</strong> 处，而 <strong>0x0 到 0x80000000</strong> 地址空间被预留给 I/O 设备。</p><h4 id="2-从机器模式开始执行内核"><a href="#2-从机器模式开始执行内核" class="headerlink" title="2. 从机器模式开始执行内核"></a>2. 从机器模式开始执行内核</h4><p>RISC-V 计算机在机器模式下启动，并执行引导加载程序的 <code>_entry</code> 函数。此时，虚拟地址直接映射到物理地址，分页硬件被禁用。</p><p>在内核代码执行前，需要设置初始堆栈空间。在 <code>kernel/start.c</code> 中，<code>_entry</code> 为堆栈声明空间并设置堆栈指针。</p><h4 id="3-从机器模式切换到管理员模式"><a href="#3-从机器模式切换到管理员模式" class="headerlink" title="3. 从机器模式切换到管理员模式"></a>3. 从机器模式切换到管理员模式</h4><p>一旦堆栈设置完成，<code>_entry</code> 会调用 C 函数 <code>start</code>，并完成一些初始化任务。随后，通过 RISC-V 的 <code>mret</code> 指令切换到 <strong>管理模式</strong>（supervisor mode），为后续的操作做准备。</p><h4 id="4-进入管理模式并进行配置"><a href="#4-进入管理模式并进行配置" class="headerlink" title="4. 进入管理模式并进行配置"></a>4. 进入管理模式并进行配置</h4><p>在管理模式下，内核继续进行系统配置，例如配置时钟芯片以触发定时器中断，为进程调度提供支持。</p><h4 id="5-初始化设备并创建第一个进程"><a href="#5-初始化设备并创建第一个进程" class="headerlink" title="5. 初始化设备并创建第一个进程"></a>5. 初始化设备并创建第一个进程</h4><p>在 <code>main</code> 函数中，内核初始化各种设备和子系统，然后调用 <code>userinit</code> 创建第一个用户进程。该进程通过执行 <code>initcode.S</code> 加载并执行 <code>/init</code> 程序。</p><h4 id="6-执行-exec-系统调用"><a href="#6-执行-exec-系统调用" class="headerlink" title="6. 执行 exec 系统调用"></a>6. 执行 <code>exec</code> 系统调用</h4><p>在 <code>initcode.S</code> 中，通过 <code>exec</code> 系统调用加载 <code>/init</code> 程序，并替换当前进程的内存映像。</p><h4 id="7-init-程序和-Shell-启动"><a href="#7-init-程序和-Shell-启动" class="headerlink" title="7. /init 程序和 Shell 启动"></a>7. <code>/init</code> 程序和 Shell 启动</h4><p>一旦 <code>/init</code> 被执行，内核返回到 <code>/init</code> 进程的用户空间。<code>/init</code> 会设置必要的设备文件并启动一个 shell，系统就此完全启动。</p>]]></content>
      
      
      <categories>
          
          <category> 操作系统 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 操作系统 </tag>
            
            <tag> MIT6.s081 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
